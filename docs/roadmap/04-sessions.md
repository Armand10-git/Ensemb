# Roadmap technique — Reproduction d'Ensemb 

**Stack cible :** NestJS · TypeScript · Prisma · PostgreSQL · Redis · BullMQ · Socket.io · React · Vite · TailwindCSS · shadcn/ui · TanStack Router · TanStack Query · Zustand · React Native · Expo · Turborepo · Docker · GitHub Actions

> Ce document remplace la version agnostique précédente. La stack étant désormais figée, chaque phase liste les modules NestJS, modèles Prisma, écrans React, files BullMQ, événements Socket.io et standards DevOps (CI/CD, conteneurisation, observabilité) concrets à livrer. Il sert à la fois de **roadmap de projet** et de **fichier de référence technique** (architecture, schéma de données, conventions) à garder ouvert pendant tout le développement.

> **Révision du 16 juillet 2026 (revue sécurité & exploitation)** — décisions ajoutées en §17 : chiffrement des secrets tenant (S), piège RLS/pooling (T), journal d'audit (U), paiements mobile money asynchrones & idempotence des webhooks (V), MFA plateforme (W), génération des références (X), stockage objet (Y), workers BullMQ dédiés (Z), versionnement d'API (AA), continuité/PITR (AB), annulation de document (AC), jalon MVP (AD), mode hors-ligne POS assumé hors périmètre (AE). Les sessions intercalaires S08b, T07b, S15b, S21b, S23b, S30b et S50b (§19) portent leur implémentation.

---


> **Extrait ciblé — Sessions de travail S01–S52b / T01–T10 (§19).** Document découpé pour lecture sélective par Claude Code ; la version intégrale fait référence.

## 19. Découpage en sessions de travail

Les phases P0–P14 (§13) donnent le grain macro ; cette section les subdivise en **52 sessions courtes** (S01–S52, complétées de 6 sessions intercalaires S08b/S15b/S21b/S23b/S30b/S50b issues de la revue sécurité & exploitation) **+ 11 sessions dédiées au multi-tenant/SaaS** (T01–T10 et T07b, Bloc B2), séquencées strictement selon le graphe de dépendances du schéma Prisma (§4) — une table n'est jamais construite avant les tables qu'elle référence par clé étrangère, et `Organization` (T01) précède tout le reste puisque chaque table métier en dépend désormais. Chaque session correspond à peu près à **un module ou une sous-fonctionnalité**, se termine par une définition de "fait" vérifiable, et se clôt idéalement par un commit. Sauf mention contraire, une session dépend de la précédente au sein de son bloc ; les dépendances *entre blocs* sont indiquées en tête de bloc.

> **Définition de "fait" générique, valable pour toute session (en plus de la colonne "Fait quand…" spécifique à chaque ligne) :**
> - **Sessions backend** (Blocs B, B2 à K) : le module livré a (1) des **tests unitaires** Jest sur la logique de service (mocks uniquement — calculs, règles de permission, conversion d'unité…) et (2) au moins un **test d'intégration** Supertest qui monte le module NestJS réel contre la Postgres/Redis de test (§16) et vérifie la réponse HTTP de bout en bout, y compris les cas d'erreur (permission refusée, entité introuvable, contrainte métier violée). Sans ces deux niveaux, la session n'est pas terminée — la colonne "Fait quand…" décrit le critère *fonctionnel*, pas une dispense de tests.
> - **Sessions frontend** (Blocs E/F/G/M, et volet frontend du Bloc B2) : au moins un test de composant/hook (Jest + Testing Library) sur la logique non triviale (ex. calcul du panier POS), et le parcours utilisateur concerné est ajouté au corpus Playwright s'il fait partie des scénarios critiques du §18.
> - **Sessions mobile** (Bloc L) : mêmes principes que web, dans la limite de l'outillage E2E mobile disponible (Detox/Maestro) — à défaut, un scénario de test manuel documenté.
> - **Toute session à partir du Bloc C** crée ses modèles Prisma avec `organizationId` dès l'écriture initiale (§17, point M) — ce n'est jamais une session de rattrapage séparée.
> - Les sessions purement infrastructurelles (Bloc A, migrations pures) sont dispensées de tests applicatifs mais doivent rester vérifiables (ex. CI verte, `docker-compose up` réussi).

> **Estimation de charge :** lors de la revue de ce document avec l'équipe, chaque session reçoit une estimation grossière (**S / M / L**) posée collectivement — sans cet ordre de grandeur, le séquencement reste une liste ordonnée, pas un plan pilotable.

> **Jalon MVP (§17, point AD) :** la fin du **Bloc E + T01–T07** constitue la première cible commercialisable — un tenant s'inscrit, vend au POS et paie son abonnement. En cas de glissement de planning, tout ce qui suit ce jalon est négociable ; rien de ce qui le précède ne l'est.

### Bloc A (= P0) — Socle & DevOps

| # | Contenu | Fait quand… |
|---|---|---|
| S01 | Init Turborepo : `apps/api`, `apps/web`, `apps/mobile` (squelettes vides), `packages/config`, `database`, `types`, `ui`, `utils`, workspaces pnpm | `turbo build` tourne sans erreur sur des apps vides | ✅ 2026-07-18 |
| S02 | `docker-compose.yml` (Postgres + Redis) + `Dockerfile` multi-stage `api`/`web` | `docker-compose up` démarre, `apps/api` se connecte à Postgres et Redis | ✅ 2026-07-18 |
| S03 | CI (`ci.yml` : lint/typecheck/test/build) + Conventional Commits/husky/commitlint + protection de `main` | CI verte sur une PR de test | ✅ 2026-07-18 — PR `chore/s03-ci-commitlint`, 4 jobs verts. Dettes : lint-staged+prettier à câbler, config ESLint par workspace à structurer (packages/config/eslint). Protection `main` à finaliser (required checks) une fois les jobs enregistrés par GitHub. |

### Bloc B (= P1+P2) — Socle backend, auth, identité (dépend du Bloc A)

| # | Contenu | Fait quand… |
|---|---|---|
| S04 | Schéma Prisma `User`, `Role`, `Permission`, `RoleOnUser`, `PermissionOnRole` (§4, avec `organizationId` sur `User`/`Role`) + migration initiale — dépend de `Organization` (T01) | `prisma migrate dev` passe, tables visibles | ✅ 2026-07-19 — migration `20260718225308_add_user_role_permission`, 3 tests d'intégration verts (création User+Role+RoleOnUser, même email dans 2 orgs OK, doublon email même org rejeté). |
| S05 | Seed : catalogue de permissions (global), rôle admin, utilisateur admin pour une organisation de démonstration (§4) | Un admin peut être créé par script, mot de passe hashé | ✅ 2026-07-19 — `packages/database/prisma/seed.ts` idempotent : 108 permissions, organisation démo, rôle Administrateur (toutes permissions), utilisateur `admin@demo.ensemb.cm` (bcrypt coût 12). |
| S06 | `AuthModule` : login JWT access+refresh, guard `isActive`, blacklist Redis, scoping par organisation | Flow §18.1 étapes 1-3 et 6 fonctionnels | ✅ 2026-07-19 — `POST /api/v1/auth/login` (access 15min + refresh 7j, permissions), `/refresh` (rotation setNx), `/logout` (blacklist Redis). 8 tests unitaires verts (anti-timing, anti-énumération, déduplication permissions, rotation). Revue sécurité post-session : DUMMY_HASH corrigé (hash bcrypt valide cost 12), Helmet+CORS+body limit 10kb+ValidationPipe whitelist en place. Dettes documentées : `isActive` révèle existence compte (B2B acceptable), RLS PostgreSQL (T03), sous-domaine tenant (T02). |
| S07 | `RolesModule` + `PermissionGuard` + `@RequirePermission()` + interceptor `records.viewAll` | Un endpoint protégé refuse sans permission ; flow §18.11 point 4 vérifiable | ✅ 2026-07-19 — `PermissionGuard` (403 neutre, scope org), `@RequirePermission()`, `ViewAllInterceptor` (flag `viewAll` injecté dans la requête). `RolesService` CRUD soft-delete + `addPermissions`/`removePermissions` + `assignRole`/`revokeRole`, tout scopé `organizationId`. `RolesController` REST paginé sous `/roles`, protégé `JwtAuthGuard + PermissionGuard`. 25 tests unitaires verts (guard, service). Build TypeScript propre. |
| S08 | `HealthModule` (`/health`,`/ready`) + `RealtimeModule` minimal (Gateway Socket.io, connexion authentifiée seulement, rooms scopées par organisation) | Probes OK ; un client socket authentifié se connecte | ✅ 2026-07-19 — `GET /health` liveness (200 statique), `GET /ready` readiness (Postgres + Redis via terminus, 503 si KO). `RealtimeGateway` namespace `/realtime` : token extrait depuis `auth.token` ou `Authorization: Bearer`, validé via `JwtService`, room `org:<organizationId>` jointée ou socket déconnecté immédiatement. Redis adapter (`@socket.io/redis-adapter`) branché dans `afterInit`. 33 tests unitaires verts. Build TypeScript propre. |
| S08b | `AuditModule` : modèle `AuditLog`, interceptor global, branchement sur les mutations sensibles déjà en place (rôles/permissions) + préfixe global `/api/v1` (§17, points U/AA) | Une modification de permissions laisse une trace `AuditLog` complète (acteur, avant/après) ; toutes les routes répondent sous `/api/v1` | ✅ 2026-07-19 — Modèle `AuditLog` (Prisma, migration `add-audit-log`). `@Auditable({ action, entity })` décorateur `SetMetadata`. `AuditInterceptor` global (`APP_INTERCEPTOR`) : fire-and-forget avec `.catch()`, sanitization des champs sensibles (password, token…), extraction `entityId` depuis la réponse ou `req.params.id`. `AuditService.create()` (try-catch interne, log serveur) + `findAll()` paginé. `setGlobalPrefix('api/v1', { exclude: ['health','ready'] })` dans `main.ts`. `@Auditable` sur `PATCH /roles/:id`, `DELETE /roles/:id`, `POST /roles/:id/permissions`, `DELETE /roles/:id/permissions`. 38 tests unitaires verts. Typecheck propre. |

### Bloc B2 — Multi-tenance & SaaS (dépend du Bloc B ; précède le Bloc C — T01 précède même S04)

| # | Contenu | Fait quand… |
|---|---|---|
| T01 | Schéma `Organization` + `PlatformAdmin` (§4), migration initiale | Table créée, contrainte d'unicité du sous-domaine testée — **doit précéder S04** | ✅ 2026-07-18 — PR `feat/t01-organization-schema`, 3 tests d'intégration verts contre Postgres. Note : test:integration non câblé en CI (service postgres à ajouter en S04+). |
| T02 | `TenancyModule` : middleware de résolution de tenant par sous-domaine, contexte de requête (`AsyncLocalStorage`), extension Prisma d'auto-scoping par `organizationId` | Une requête sur un sous-domaine ne retourne jamais les données d'un autre tenant (test d'isolation) | ✅ 2026-07-19 — `TenantContextService` (AsyncLocalStorage, lève InternalServerErrorException hors contexte), `TenancyService` (résolution partagée Redis TTL 1h + fallback Prisma), `TenancyMiddleware` (validation RFC 1123 du sous-domaine, 404 neutre), token `PRISMA_TENANT_CLIENT` (`$extends` branché sur le client réel, auto-scope findMany/findFirst/findUnique/count sur User/Role/AuditLog), `GET /api/v1/public/organizations/by-subdomain/:subdomain` (@Throttle 10 req/min, validation format). Routes exemptes : /health, /ready, /api/v1/auth/*, /api/v1/public/*. 64 tests unitaires verts. Build TypeScript strict vert. Post-revue : extension Prisma branchée, logique dédupliquée dans TenancyService, validation subdomain RFC 1123, rate limiting anti-énumération. |
| T03 | Row-Level Security PostgreSQL en défense en profondeur (policy `organization_id = current_setting(...)`) | Une requête SQL brute sans passer par Prisma respecte quand même l'isolation | ✅ 2026-07-19 — Migration `add_rls_tenant_isolation` : RLS + FORCE RLS activés sur `users` et `roles` ; policy `NULLIF(current_setting('app.current_tenant', true), '')::uuid` (fail-closed sur chaîne vide). Rôle `ensemb_app` (non-superuser) créé pour soumettre l'app aux policies (POSTGRES_USER est superuser → bypass RLS sans rôle dédié). `PrismaService.withTenant()` : helper transactionnel posant `set_config('app.current_tenant', id, true)` (équivalent paramétrable de SET LOCAL, scoped à la transaction). 4 tests d'isolation verts contre Postgres réel : visibilité par tenant, fail-closed sans contexte, absence de fuite SET LOCAL après commit. |
| T04 | Flow d'inscription (§18.0) : création `Organization` + premier utilisateur admin + validation de disponibilité du sous-domaine ; calcul de `trialEndsAt` selon la fenêtre de lancement (voir T06) | Un nouveau tenant est immédiatement utilisable après inscription, avec un plan d'essai | ✅ 2026-07-19 — Migration `add_trial_fields_to_organizations` (`trialEndsAt DateTime?`, `trialEndedReason String?`). `RegistrationModule` : `POST /api/v1/public/organizations/register` (transaction atomique Organization + Role "Administrateur" + User admin + RoleOnUser, bcrypt cost 12, `trialEndsAt = computeTrialEndsAt(now, 30j)`), `GET /api/v1/public/organizations/check-subdomain/:subdomain` (réponse neutre `{ available: boolean }`, anti-énumération). DTO zod avec liste noire 11 sous-domaines réservés + RFC 1123. Rate limiting : 5 req/min (register), 20 req/min (check-subdomain). `@Auditable(ORGANIZATION_REGISTER)`. Routes exemptées du middleware tenant via `/api/v1/public/.*`. Post-revue : race condition P2002 catchée dans `$transaction` (inscriptions simultanées → 409 propre au lieu de 500) ; `computeTrialEndsAt()` extraite en fonction pure testée directement ; `AuditInterceptor.extractEntityId` élargi à `organizationId`/`userId`/`roleId` (audit ORGANIZATION_REGISTER capte l'entityId) ; test e2e de concurrence réelle (`Promise.all` simultanés → exactement 1 réussit). 109 tests unitaires + 8 e2e verts. Lint + typecheck stricts verts. Dette soldée en T06 : `computeTrialPeriod()` + `Subscription TRIALING` + `PlatformSetting.launchPromoEndsAt`. |
| T05 | Branding : `logoUrl`/`primaryColor` sur `Organization`, endpoint public `GET /public/organizations/by-subdomain/:subdomain` | Flow §18.14 complet ; deux tenants affichent deux thèmes différents sans redéploiement | ✅ 2026-07-19 — Champs `logoUrl`/`primaryColor` déjà présents dans le schéma (aucune migration). `OrganizationsModule` : `PATCH /api/v1/organizations/branding` (JwtAuthGuard + PermissionGuard `organization.branding.edit`, zod `UpdateBrandingSchema` : `url().max(2048)` + regex `#[0-9A-Fa-f]{6}`, `@Auditable(ORGANIZATION_BRANDING_UPDATE)`, spread conditionnel sans mass assignment, emit Socket.io `organization:brandingUpdated` vers room `org:<organizationId>`). Endpoint public `GET /api/v1/public/organizations/by-subdomain/:subdomain` enrichi : `logoUrl` et `primaryColor` dans la réponse (aucun champ sensible). `TenancyService.resolvePublicOrganization()` ajoutée. 123 tests unitaires + 9 tests e2e branding (401/403/422/200/isolation) verts. Lint + typecheck stricts verts. |
| T06 | `Plan` + `Subscription` + `PlatformSetting` (§4), seed des plans par défaut (avec `trialDurationDays`/`trialRevenueCapAmount`), écran admin pour configurer la fenêtre de lancement, `QuotaGuard` sur les endpoints de création | Dépasser un quota renvoie une erreur explicite, pas une erreur 500 ; la fenêtre de lancement est modifiable sans déploiement (§17 point R) | ✅ 2026-07-19 — Migrations : `add_plan_subscription_platform_setting` (modèles `Plan` avec prix `Decimal(10,2)`, quotas `Int?` nullable ; `Subscription` avec enum `SubscriptionStatus`, `@unique organizationId` ; `PlatformSetting` clé/valeur JSON) + `add_index_subscription_plan_id` (`@@index([planId])` sur `subscriptions` — index FK absent par défaut sous Prisma). Seed idempotent : 3 plans (starter 5 000/50 000 XAF, pro 15 000/150 000 XAF, enterprise 40 000/400 000 XAF avec plafonds CA et quotas) + `PlatformSetting.launchPromoEndsAt = "2026-09-30T23:59:59Z"`. `RegistrationService` : `computeTrialEndsAt()` remplacé par `computeTrialPeriod(now, launchPromoEndsAt, trialDurationDays)` — fonction pure qui retourne `new Date(launchPromoEndsAt.getTime())` (copie, pas référence) pendant la fenêtre, et `now + trialDurationDays` sinon ; lecture de `PlatformSetting` + plan starter dans la transaction via `Promise.all` ; validation `typeof parsed === 'string'` + `!isNaN(d.getTime())` avant usage (pas de cast naïf) ; `Subscription TRIALING` créée atomiquement. `BillingModule` : `BillingService.getSubscription()` (NotFoundException si absente, loggée côté serveur) + `getPlatformSetting()` (validation runtime `typeof string`, InternalServerErrorException générique côté client) ; `QuotaGuard` + `@CheckQuota('users'|'warehouses'|'products')` — quota null = illimité sans count, 403 explicite en français avec nom de plan et limite, 500 générique sur erreur interne, `Pick<Plan>` pour le typage ; importé dans `AppModule`. Post-revue : `try/finally` dans le test e2e "après la fenêtre" pour garantir la restauration de `PlatformSetting` même en cas d'échec ; TOCTOU `count`/`create` documenté comme dette à adresser dans `UsersModule` (Bloc C). 142 tests unitaires verts ; 22 tests e2e verts (billing ×6 — fenêtre/post-fenêtre/quota-ok/403/illimité/401 ; registration ×9 mis à jour avec cleanup Subscription). Lint + typecheck stricts verts. Checklist §15 : quota 403 ✅ ; `PlatformSetting` modifiable en base ✅ ; écran admin (T08) et surveillance CA (T07) encore pendants. |
| T07 | Intégration de l'agrégateur de paiement pour la facturation récurrente : lien de paiement à la souscription, webhook de confirmation, `billing-queue` pour les échéances suivantes **et** pour la vérification du plafond de CA d'essai après chaque vente | Flow §18.15 complet en mode test de l'agrégateur, y compris la coupure anticipée par le CA (étape 6) | ✅ 2026-07-19 — Migration `add_invoice_webhook_event` (`Invoice` avec `amount Decimal(10,2)`, `period`, `status InvoiceStatus` ; `WebhookEvent` avec `@@unique([provider, providerEventId])`). `PaymentAggregatorService` : `generatePaymentLink()` + `verifyWebhookSignature()` HMAC-SHA256 ; mode test (NODE_ENV=test ou variables absentes) → stub sans appel HTTP réel. `BillingService` étendu : `createPaymentLink()` (Invoice PENDING + job `invoice.expire` dans 24h), `confirmPayment()` (idempotent — PAID ignoré), `expireInvoice()` (PENDING → FAILED, Subscription PAST_DUE), `checkTrialCap()` (CA cumulé via `invoice.aggregate` >= `trialRevenueCapAmount` → PAST_DUE + trialEndedReason). `BillingController` : `POST /api/v1/billing/subscribe` (JwtAuthGuard + `billing.manage` + `@Auditable`). `WebhookController` : `POST /api/v1/webhooks/billing` — corps lu en Buffer brut (`rawBody: true` dans NestFactory.create), vérification HMAC avant tout accès DB, persistance `WebhookEvent` avant traitement (idempotence P2002), 200 systématique même en erreur interne. `BillingWorker` dans `apps/api/src/workers/` : jobs `invoice.expire` / `invoice.renew` / `billing.checkTrialCap`, chacun avec `organizationId`, jamais cross-tenant. `BullModule.forRootAsync` dans AppModule (Redis URL depuis ConfigService). `RealtimeGateway.afterInit` rendu résilient (garde server?.adapter). 172 tests unitaires verts (BillingService ×15 — Decimal, idempotence, checkTrialCap ; WebhookController ×9 — HMAC 401, rejeu P2002, erreur→200, ordre webhookEvent→confirmPayment ; PaymentAggregatorService ×7 — HMAC valide/invalide/vide/payload altéré, liens fictifs uniques). 4 tests e2e verts (billing, billing-webhook, registration, branding). Lint + typecheck stricts verts. Post-revue (round 2) : `BillingWorker` sorti du process HTTP → `WorkerModule` + `src/worker.ts` (entrypoint dédié, §17 point Z) ; `amount` e2e `new Decimal('5000')` plus jamais de Float littéral (§17 point A). `createPaymentLink()` valide `planId === subscription.planId` (IDOR) et utilise `subscription.planId` dans le job (pas la valeur cliente) ; `confirmPayment()` calcule `newPeriodEnd` depuis `Math.max(now, currentPeriodEnd)` (renouvellement anticipé correct) ; `checkTrialCap()` retourne `boolean` → élimine le double `getSubscription()` dans le worker ; `delay invoice.renew = Math.max(0, periodMs - MS_7D)` ; `BillingJobData.period` typé `'monthly' | 'annual'`. `PaymentAggregatorService` : imports `crypto` fusionnés, `amount.toString()` (pas `.toNumber()`), URL agrégateur via `PAYMENT_AGGREGATOR_BASE_URL`. Erreur 422 générique sur `billing/subscribe` (plus d'exposition des `fieldErrors`). `WebhookController` : résout `organizationId` via invoice avant la création du `WebhookEvent` ; marque `processedAt` après traitement (best-effort). `RealtimeGateway` : CORS WebSocket `origin: '*'` → whitelist `*.monapp.cm + localhost`. Schema : `WebhookEvent.processedAt` renommé `receivedAt` (horodatage de réception), nouveau `processedAt DateTime?` (fin de traitement), `organizationId String?` ajouté (§17 point M) ; migration `20260719200000_webhook_event_timestamps_orgid`. 45 tests unitaires verts ; typecheck et lint stricts verts. |
| T07b | Idempotence des webhooks : modèle `WebhookEvent`, garde d'unicité `(provider, providerEventId)` sur `/webhooks/billing` et `/webhooks/payments/:organizationId` + service de chiffrement applicatif des secrets tenant (`APP_ENCRYPTION_KEY`, AES-256-GCM) (§17, points S/V) | Un webhook rejoué à l'identique est acquitté sans effet métier ; un dump SQL brut ne révèle aucun secret en clair | ✅ 2026-07-19 — Correction du bug de nommage de colonne dans la migration `20260719200000_webhook_event_timestamps_orgid` (`"processedAt"` camelCase au lieu de `"processed_at"` snake_case, non appliquée). Migrations : `20260719173944_add_smtp_server` (`SmtpServer` avec `passwordCipher TEXT` pour le secret AES-256-GCM, `@@unique organizationId`). `EncryptionService` (AES-256-GCM, 12 bytes IV, 16 bytes authTag, format `iv:authTag:ciphertext` hex) + `EncryptionModule` (`@Global`) importé dans `AppModule` — clé lue via `ConfigService.getOrThrow('APP_ENCRYPTION_KEY')` + hash SHA-256 normalisé 32 octets, erreur au boot si absente. `SmtpServerService` : `upsert()` chiffre `password` avant écriture, retourne un DTO sans `passwordCipher` ni `password` ; `getDecryptedPassword()` usage interne uniquement. `SmtpModule` + endpoint `PUT /api/v1/organizations/smtp` (JwtAuthGuard + `organization.settings.edit` + `@Auditable`). `WebhookController` : méthode privée `persistWebhookEvent()` partagée — suppression du cast `any` (Prisma client régénéré après migration) ; ajout `POST /api/v1/webhooks/payments/:organizationId` (stub POS mobile money — loggue sans appel à PosModule, idempotent via le même pattern P2002). Tests : 196 unitaires verts (EncryptionService ×12 — round-trip, IV aléatoire, format, clé invalide, format invalide, boot sans clé ; SmtpServerService ×6 — chiffré en base, DTO sans secret, round-trip, idempotence, IV différent chaque upsert, NotFoundException ; idempotence webhook ×8 — billing et POS : premier appel, rejeu P2002, HMAC invalide) ; 35 e2e verts dont 7 nouveaux (PUT smtp 200/401, anti-fuite dump SQL SELECT brut, webhook billing rejoué ×2, webhook POS rejoué ×2). Lint + typecheck stricts verts. |
| T08 | `PlatformAdminModule` : auth séparée **avec MFA TOTP obligatoire (§17, point W)**, liste des organisations, suspension/réactivation, tableau de bord (MRR, conversion, churn, factures en échec — agrégats mis en cache) | Flow §18.16 complet ; un compte tenant normal ne peut jamais y accéder, même en devinant l'URL ; la connexion sans second facteur TOTP est refusée | ✅ 2026-07-19 — Migration `add_platform_admin_totp` (champs `totpSecret String?`, `totpEnabled Boolean`, `isActive Boolean` sur `PlatformAdmin`). `PlatformAdminAuthService` : login anti-timing (DUMMY_HASH bcrypt), tempToken JWT `PLATFORM_JWT_SECRET` (step `mfa`\|`totp-setup`, TTL 5 min), `setupTotp()` secret AES-256-GCM, `verifyTotp()` via `verifySync({token, secret, crypto: NobleCryptoPlugin, base32: ScureBase32Plugin})`, refresh avec blacklist Redis + rotation, logout. `PlatformAdminGuard` : vérifie `PLATFORM_JWT_SECRET`, rejette `payload.step` présent (token temp) et `isActive=false`. `TempTokenGuard` factory par step autorisé. `PlatformAdminDashboardService` : MRR `Decimal` (jamais Float), cache Redis 10 min `platform:dashboard:metrics`. `PlatformAdminOrganizationsService` : liste paginée, `suspendOrganization()` (Redis `platform:org-suspended:<id>` TTL 7j + AuditLog), `reactivateOrganization()` (del Redis + AuditLog). `JwtRefreshStrategy` bloque le refresh si clé suspension présente. `TenancyMiddleware` exempte `/api/v1/platform-admin/**`. 26 suites / 220 tests verts, lint et typecheck stricts verts. PR : feat/t08-platform-admin-mfa. |
| T09 | Redesign du backup : export de données par organisation (CSV/JSON) ; le `pg_dump` complet devient une tâche d'exploitation plateforme | Flow §18.12 complet ; un tenant ne peut exporter que ses propres données | ✅ 2026-07-19 — Migration `add_backup_export` (modèle `BackupExport` avec enums `ExportStatus` / `ExportFormat`, index sur `[organizationId, requestedAt DESC]`). `BackupService` : `requestExport()` (PENDING + job BullMQ `export.generate`), `listExports()` (paginé, sans `errorMessage`), `getDownloadStream()` (ownership vérifié, ReadStream uniquement), `deleteExport()` (fichier physique d'abord, BDD ensuite), `purgeOldExports()` (rétention 30j). `BackupController` : 4 routes (POST/GET/GET download/DELETE), `JwtAuthGuard + PermissionGuard`, `@RequirePermission('backup.manage')`, `@Auditable` sur POST ; `organizationId` depuis `req.user` uniquement (anti-IDOR). `BackupModule` importé dans `AppModule`. `BackupWorker` : job `export.generate` (PROCESSING → CSV/JSON via `csv-stringify` → fichier local `storage/exports/<orgId>/<exportId>.<ext>` → COMPLETED + Socket.io `backup:completed`) + job `export.purge` ; intégré dans `WorkerModule`. Permission `backup.manage` ajoutée au seed. Stockage local = dette documentée `// TODO T09-debt: migrer vers S3-compatible (S13, §17 point Y)`. 12 tests unitaires verts (BackupService ×9, BackupWorker ×3), 7 tests e2e verts (401/403/201/liste/téléchargement/isolation tenant/204 suppression). Lint + typecheck stricts verts. |
| T10 | `apps/marketing` : accueil, tarifs, FAQ, CTA vers `signup.tsx` de `apps/web` ; formulaire "se connecter" qui redirige vers `{subdomain}.monapp.com/login` | Flow §18.17 complet ; déployable indépendamment de `apps/web`/`apps/api` | ✅ 2026-07-19 — `apps/marketing` React + Vite + TanStack Router + Tailwind CSS. Pages `/` (HeroSection, 4 features, FindOrgForm) et `/pricing` (toggle mensuel/annuel, 3 plans Starter/Pro/Enterprise en XAF avec `Intl.NumberFormat`). `FindOrgForm` : validation regex RFC 1123 + anti-tirets-consécutifs, redirection `https://{subdomain}.{VITE_ROOT_DOMAIN}/login` côté client uniquement, accessible (label + `aria-describedby`). `VITE_APP_URL` / `VITE_ROOT_DOMAIN` lus depuis `import.meta.env` — aucune valeur codée en dur. 25 tests vitest + Testing Library verts (FindOrgForm ×16 dont régression majuscules/double-tirets ; PricingCard ×9 dont toggle). `turbo build --filter=@ensemb/marketing` passe seul sans `apps/api` ni `apps/web`. TypeScript strict vert. Dette documentée : `TODO T10 — extraire regex dans packages/validation` ; pas de pré-sélection de plan au signup (T11+). |

### Bloc C (= P3) — Référentiels (dépend des Blocs B et B2 ; ordre interne = ordre des FK)

| # | Contenu | Fait quand… |
|---|---|---|
| S09 | `Currency` (+ `symbolPosition`) + `Warehouse` : schéma, CRUD, écrans `settings/currencies.tsx` / `warehouses.tsx` | CRUD complet des deux, testé à l'écran | ✅ 2026-07-20 — Migration `add_currency_warehouse` : modèle `Currency` (global plateforme, `isActive` soft-disable, `symbolPosition` BEFORE/AFTER) + modèle `Warehouse` (scopé tenant, `isDefault` transactionnel, `version Int` pour verrouillage optimiste S15, soft-delete `deletedAt`, index unique partiel SQL `WHERE "deletedAt" IS NULL`). Seed : upsert XAF + `organization.settings.edit` permission. `CurrencyModule` : GET public, POST/PATCH/DELETE `PlatformAdminGuard`, PATCH `/organizations/default-currency` JwtAuthGuard + permission. `WarehouseModule` : CRUD complet JwtAuthGuard + permissions, protection du dernier entrepôt actif (`BadRequestException`), `ConflictException` P2002 capturée. Frontend : `CurrenciesPage` + `WarehousesPage` avec TanStack Query, 5 états chacune (skeleton, vide, erreur, partiel, succès), AlertDialog destructif nommant l'objet. Tests : 25 unitaires `warehouse.service.spec.ts`, 11 unitaires `currency.service.spec.ts`, 11 e2e Supertest 11/11 verts, 11 Vitest composants 11/11 verts. Lint + typecheck stricts verts (30 suites, 262 tests unitaires API). |
| S10 | `Category` + `Brand` : schéma, CRUD, écrans | Idem | ✅ 2026-07-20 — modèles `Category`, `Brand`, stub `Product` (FK ready pour S14), migration `20260720111122_add_category_brand` (3 index uniques partiels SQL raw). `CatalogModule` : `CategoryService` + `BrandService` CRUD soft-delete, `CategoriesController` + `BrandsController` sous `/api/v1/catalog/`, `PermissionGuard` + `@Auditable`. Écrans `settings/categories.tsx` (recherche locale, badge monospace code, remove bloqué si produits actifs) et `settings/brands.tsx` (avatar initiale, tooltip upload S13). 33 tests unitaires + 20 e2e + 17 composants verts. Dette documentée : upload logo réel S13 (`TODO S10-debt`). |
| S11 | `Unit` (hiérarchie base/opérateur) + tests unitaires de conversion | Conversion carton=12×pièce testée et correcte | ✅ 2026-07-20 — Migration `20260720140000_add_unit` : modèle `Unit` (auto-référentiel, `operator` `"*"`/`"/"`, `operatorValue Decimal(14,6)`, soft-delete, 2 index uniques partiels SQL raw sur `(organizationId, name)` et `(organizationId, shortName) WHERE "deletedAt" IS NULL`). FK `unitId`/`unitSaleId`/`unitPurchaseId` ajoutées au stub `Product`. `packages/utils` : `convertToBase()` + `convertFromBase()` avec `decimal.js` — 9 tests Jest verts (opérateurs `*` et `/`, valeurs décimales, aller-retour). `UnitService` : `findAll`/`findOne`/`create`/`update`/`remove` scopés tenant, garde hiérarchie max 1 niveau (BadRequestException si parent déjà dérivé), ConflictException sur P2002 (name/shortName identifié), soft-delete bloqué si sous-unités ou produits actifs (stub TODO S14). `UnitsController` sous `/api/v1/catalog/units` : 5 routes JwtAuthGuard + PermissionGuard + @Auditable, `organizationId` extrait de `req.user`. Seed : Pièce, Carton (×12), Litre, Centilitre (÷100) pour l'org démo. Frontend : `settings/units.tsx` 5 états (skeleton, vide, erreur, partiel, succès), tableau avec badge hiérarchie "Carton = 12 × Pièce", Dialog avec switch "Unité dérivée", aperçu en direct, AlertDialog suppression. 19 tests unitaires UnitService, tests e2e (CRUD + hiérarchie profonde 400 + isolation tenant + doublon 409), 9 tests Vitest composant (aperçu conversion, switch dérivé, 5 états). 314 tests API unitaires verts, 37 tests frontend verts, lint + typecheck stricts verts. |
| S12 | `PartnersModule` : `Client` + `Provider`, import CSV, export Excel (`excel-queue`) | Import d'un CSV modèle et export Excel fonctionnels |

### Bloc D (= P4) — Produits & stock (dépend du Bloc C)

| # | Contenu | Fait quand… |
|---|---|---|
| S13 | `UploadsModule` (`multer` + `sharp`), testé isolément | Upload d'une image redimensionnée fonctionnel avant tout branchement | ✅ 2026-07-21 — PR `feat/s13-uploads-module` ; StorageService (S3/MinIO), UploadsService (magic bytes file-type + sharp re-encode), UploadsController (POST + GET signed-url, IDOR check) ; 17 tests unitaires + tests e2e MinIO ; file-type v16 CJS. |
| S14 | `Product` + `ProductVariant` : schéma, CRUD, code-barres, upload branché | Création d'un produit avec variante et image | ✅ 2026-07-21 — PR `feat/s14-product-variant` ; modèles `Product`/`ProductVariant`/`ProductWarehouse` (Decimal, soft-delete, index partiels NULL-safe) ; migration SQL additive ; DTOs zod ; ProductService (findAll paginé, create transaction, uploadImage S3, remove atomique, createVariant/removeVariant) ; IDOR checkFkOwnership sur toutes les FK ; 8 endpoints REST (ParseUUIDPipe, Auditable, FileInterceptor 5 Mo) ; écran React 5 états UX + jsbarcode + avatar S3 ; 20 unitaires + 17 e2e + 8 composants frontend. |
| S15 | `ProductWarehouse` (stock par entrepôt) + colonne `version` posée (§17 point B) | Quantité par entrepôt consultable, `version` incrémentable | ✅ 2026-07-21 — `InventoryModule` : `ProductWarehouseService` (findByProduct, findByWarehouse paginé, initStock idempotent, getStockSummary, adjustStock avec OptimisticLockException) + `InventoryController` (3 endpoints JwtAuthGuard+PermissionGuard : GET stock/product/:id, GET stock/warehouse/:id, POST stock/init) ; colonne Stock lazy (popover shadcn/ui) dans products.tsx. Tests : 8 unitaires `product-warehouse.service.spec.ts`, 8 e2e Supertest `product-warehouse.e2e.spec.ts` (init, idempotence, 401, 400, lecture, isolation tenant). Lint + typecheck verts. |
| S15b | `DocumentCounter` : génération transactionnelle des références par `(organizationId, documentType)` (§17 point X) + vérification des index composites et index uniques partiels sur les tables documentaires (§4) | Test de concurrence : N créations simultanées produisent N références distinctes, sans collision ni trou inexpliqué | ✅ 2026-07-21 — Schéma Prisma : enum `DocumentType` + modèle `DocumentCounter` (clé composite `@@id([organizationId, documentType, year])`) appliqué via `prisma db push`. `formatReference(type, year, counter)` dans `packages/utils` (type local, sans dépendance Prisma) + export depuis `index.ts`. `DocumentCounterService` (`nextReference(tx, orgId, type, year?)` via upsert atomique `lastCounter: { increment: 1 }` — jamais SELECT+UPDATE séparés) ; `DocumentCounterModule` `@Global()` importé dans `AppModule`. 9 index SQL partiels appliqués (`idx_products_org`, `idx_clients_org`, … `idx_product_warehouse_warehouse`). Tests : 5 unitaires `document-reference.spec.ts`, 5 unitaires `document-counter.service.spec.ts`, 2 e2e (dont concurrence N=20 Promise.all → 20 références distinctes VTE-2026-000001 à VTE-2026-000020, aucune collision ni trou). Lint + typecheck stricts verts (40 suites unitaires, 380 tests). |
| S16 | `InventoryModule` — Ajustements (+détails) | Flow §18.8 complet, `stock:updated` émis | ✅ 2026-07-21 — Schéma Prisma : enums `AdjustmentStatus` + `AdjustmentDetailType`, modèles `Adjustment` + `AdjustmentDetail` ; migration SQL `20260721160000_add_adjustment` appliquée via `prisma db execute` + `prisma generate`. `AdjustmentService` (`create` en DRAFT avec `DocumentCounterService.nextReference` dans la transaction, `validate` en transaction Serializable avec `adjustStock` verrouillage optimiste → ConflictException si conflit, `findAll`/`findOne`/`remove` soft-delete DRAFT uniquement). `AdjustmentController` (5 routes `/api/v1/inventory/adjustments`). `InventoryModule` mis à jour (+ `RealtimeModule`). Référence générée `AJT-YYYY-NNNNNN`. `stock:updated` émis après transaction ; `stock:lowAlert` émis sur soustraction ≤ seuil (`// TODO S18: persister dans Notification`). Écran frontend `routes/adjustments/index.tsx` : 5 états + Sheet création/détail + AlertDialog suppression + Socket.io `stock:updated`. Tests : 10 unitaires + 10 e2e (dont ADDITION+SOUSTRACTION net+2, isolation tenant, idempotence validation) + 8 frontend. Lint + typecheck stricts verts (41 suites unitaires, 391 tests). |
| S17 | `InventoryModule` — Transferts (+détails), atomicité source/destination | Flow §18.9 complet, testé avec échec simulé sur l'entrepôt destination | ✅ 2026-07-21 — Schéma Prisma : enum `TransferStatus`, modèles `StockTransfer` + `StockTransferDetail` (relations nommées "TransferFrom"/"TransferTo" sur `Warehouse`) ; back-relations ajoutées sur `Organization`, `User`, `Product`, `Warehouse` ; appliqué via `prisma db push`. `TRANSFER` (préfixe `TRF`) déjà présent dans `DocumentType`. Seed : `transfers.validate` ajouté. `StockTransferService` (`create` DRAFT avec `nextReference` dans tx, `validate` en tx Serializable — `adjustStock` ×2 par ligne (source−, destination+), ConflictException sur OptimisticLock/P2034, `stock:updated` ×2 + `stock:lowAlert` si seuil source atteint, `findAll`/`findOne`/`remove` soft-delete DRAFT uniquement). `StockTransferController` (5 routes `/api/v1/inventory/transfers`, permissions `transfers.*`, `@Auditable` sur validate + delete). `InventoryModule` mis à jour. Écran frontend `routes/transfers/index.tsx` : 5 états + Sheet création (sélecteur destination exclut la source) + vue détail + AlertDialog + Toast + Socket.io `stock:updated`. Tests : 13 unitaires `stock-transfer.service.spec.ts` + 11 e2e `transfer.e2e.spec.ts` (dont rollback si dest absent, isolation tenant, revalidation 400) + 6 frontend `transfers.spec.tsx`. Lint + typecheck stricts verts (42 suites unitaires, 404 tests unitaires). |
| S18 | Alerte de stock bas + modèle `Notification` persistant | Flow §18.10 complet (émission + persistance + lecture) | ✅ 2026-07-21 — Schéma Prisma : modèle `Notification` (id, organizationId, userId, type, payload Json, readAt, createdAt) + back-relations sur `Organization` et `User` + index `(organizationId, userId, readAt)` ; migration SQL `20260721200000_add_notification` appliquée via `prisma db execute` + `prisma generate`. `RealtimeGateway` : `handleConnection` rejoint aussi la room personnelle `org:{id}:user:{userId}`. `NotificationService` : `createForOrg` (recherche utilisateurs actifs par permission via leurs rôles, `createMany`, `emit notification:new`), `findAll` paginé, `markAsRead` (anti-IDOR), `markAllAsRead`, `countUnread`. `NotificationController` (4 routes `/api/v1/notifications` : GET liste, GET unread-count, PATCH :id/read, PATCH read-all). `NotificationModule` exporté pour `InventoryModule`. TODO S18 remplacés dans `AdjustmentService.validate` et `StockTransferService.validate` par `createForOrg` fire-and-forget (`.catch` logger). Frontend : `NotificationBell` (badge rouge compteur, Popover panel 20 notifications, Tout marquer comme lu, Socket.io `notification:new`). Tests : 8 unitaires `notification.service.spec.ts` + 8 e2e `notification.e2e.spec.ts` (flow complet §18.10, isolation tenant) + 3 frontend `notifications.spec.tsx`. Lint + typecheck stricts verts (43 suites unitaires, 412 tests unitaires). |

### Bloc E (= P5) — Ventes & POS, le cœur (dépend du Bloc D)

| # | Contenu | Fait quand… |
|---|---|---|
| S19 | `Sale` + `SaleDetail` : schéma, CRUD sans paiement ni décrément de stock | Création d'une vente "brouillon", totaux calculés côté serveur | ✅ 2026-07-26 — Schéma Prisma : enums `PaymentStatus` + `DocumentStatus`, modèles `Sale` + `SaleDetail` + stub minimal `PaymentSale` (id, saleId, createdAt — champs métier ajoutés en S20 par colonnes additives, sans migration destructive) ; back-relations sur `Organization`, `Client`, `Warehouse`, `User`, `Product` ; index `(organizationId, userId/clientId/warehouseId)` pour les filtres de `findAll` (§17 — index requis sur toute requête filtrée) ; migrations `20260726162317_add_sale` + `20260726190000_add_sale_indexes`. `SaleService` (`create` en PENDING avec `DocumentCounterService.nextReference` + ownership `clientId`/`warehouseId`/`productId`/`productVariantId`/`saleUnitId` vérifiée DANS la transaction, totaux Decimal calculés côté serveur — ligne : `subTotal ± taxe ± remise` selon `taxMethod`/`discountMethod` ; en-tête : `taxRate` toujours pourcentage, `discount` toujours montant fixe XAF, faute de colonnes `taxMethod`/`discountMethod` côté en-tête — seule lecture cohérente avec les champs exposés par le DTO. `findAll` avec filtre `userId` piloté par `viewAll` (`ViewAllInterceptor`, 1ère utilisation réelle depuis son introduction S07), `update`/`remove` réservés au statut PENDING (remplacement des lignes `deleteMany`+`createMany`), émission `sale:created` si `isPos=false`. `SalesController` (`/api/v1/sales`, 5 routes ; `@Auditable` sur edit/delete uniquement — pas sur create, conformément à la convention établie par `AdjustmentController`/`StockTransferController`). Écran frontend `routes/sales/index.tsx` : 5 états + Sheet création (lignes + totaux indicatifs) + vue détail + AlertDialog suppression, branché dans `router.tsx`/`AppLayout.tsx`. Tests : 11 unitaires `sale.service.spec.ts` + 11 e2e `sale.e2e.spec.ts` (référence, calcul Decimal percentage/fixed, IDOR client/entrepôt, `viewAll`, immuabilité COMPLETED) + 5 frontend `sales.spec.tsx`. **Revue best-practices demandée en fin de session — 3 correctifs hors périmètre strict S19, tous validés et vérifiés verts :** (1) `TenancyMiddleware.extractSubdomain` traitait l'IP `127.0.0.1` (hôte par défaut de `supertest`) comme un sous-domaine `"127"`, court-circuitant le fallback `X-Organization-Id` — garde IPv4 ajoutée + mock `tenancy.middleware.spec.ts` corrigé ; (2) 16 des 20 suites e2e du projet étaient rouges (issu du fix S18 `9ec86db` : `JwtAuthGuard` dépend désormais de `TenantContextService`/`TenancyModule`, non importé par ces suites) — corrigées (import `TenancyModule` + header `X-Organization-Id`), `auth.e2e.spec.ts` déplacé de `src/modules/auth/test/` vers `apps/api/test/` (seul spec hors de ce dossier, cause d'une violation `rootDir` au typecheck) ; (3) régression de production découverte en creusant (2) : `TenancyMiddleware` interceptait aussi `/api/v1/webhooks/*` (absent de `EXEMPT_ROUTES`), cassant potentiellement la confirmation de paiement d'abonnement SaaS (`PAYMENT_AGGREGATOR_CALLBACK_URL` sans sous-domaine tenant) — route exemptée, sécurité déjà assurée par signature agrégateur + idempotence `WebhookEvent`. Détail complet dans `docs/roadmap/02-plan-et-decisions.md` (§14). `pnpm test:e2e` : 20/20 suites, 194/194 tests verts. Lint + typecheck stricts verts sur l'ensemble du projet (0 erreur). |
| S20 | `PaymentSale` + calcul automatique de `paymentStatus` | Flow §18.5 complet sur les ventes | ✅ 2026-07-27 — Schéma Prisma : stub `PaymentSale` (S19) complété par colonnes additives (`organizationId` dénormalisé, `userId`, `date`, `reference`, `amount`, `method`, `change`, `notes`, `updatedAt`) + enum `PaymentMethod` ; migration SQL `20260727100000_add_payment_sale_fields`. **Décisions tranchées en début de session** : (1) référence générée via `DocumentCounterService.nextReference` avec un nouvel enum `DocumentType.PAYMENT_SALE` (préfixe `PAY`, séquence globale par organisation, ex. `PAY-2026-000001`) plutôt qu'une dérivation `${sale.reference}-P${n}` — conformité stricte à la règle non négociable #4 (jamais de `max+1`/`count()` comme source de référence) ; ajout propagé à `packages/utils/src/document-reference.ts` (type local + table `PREFIXES` + test) ; (2) suppression physique du paiement (pas de soft-delete) — `PaymentSale` n'a pas de `DocumentStatus` propre au sens de la règle §17 point 7, traçabilité assurée par `AuditLog` via `@Auditable` sur la route DELETE. `PaymentSaleService` (`create`/`findAllForSale`/`update`/`remove`, ownership de la vente vérifiée DANS la transaction, solde restant = `grandTotal − paidAmount` jamais dépassable côté serveur même si le frontend borne déjà la saisie, recalcul de `Sale.paidAmount`/`paymentStatus` depuis la somme réelle des paiements après chaque mutation — jamais saisi). `PaymentSaleController` (4 routes sous `/api/v1/sales`, permissions `paymentSales.*`, `@Auditable` sur create/edit/delete), wiring dans `SalesModule` existant (pas de nouveau module). Écran frontend : section "Paiements" dans `SaleDetailView` (historique chronologique, bouton masqué si `PAID`) + Sheet "Ajouter un paiement" (raccourci "Solder" pré-remplissant le solde, montant borné au blur — jamais rejeté au submit, `NativeSelect` méthode) branchés dans `routes/sales/index.tsx`. Revue sécurité dédiée (IDOR, Decimal, immutabilité serveur, `@Auditable`) : aucun finding. Tests : 16 unitaires `payment-sale.service.spec.ts` + 12 e2e `payment-sale.e2e.spec.ts` (recalcul UNPAID→PARTIAL→PAID puis redescente après suppression, dépassement de solde rejeté à la création et à la modification, isolation tenant sur `saleId` et sur `:id` de paiement) + 6 frontend (`sales.spec.tsx`). `pnpm test` (tous packages) + `pnpm test:e2e` : 100 % verts, aucune régression (utils 15/15, API unitaires 439/439, API e2e 206/206 sur 21 suites, web 86/86). Lint + typecheck stricts verts sur tout le monorepo. |
| S21 | Décrément de stock + verrouillage optimiste (`version`, transaction `Serializable`) branché sur la validation d'une vente | Test de concurrence : deux ventes simultanées sur le dernier exemplaire, une seule réussit | ✅ 2026-07-27 — `SaleService.validate(id, organizationId)` (patron exact `StockTransferService.validate()`) : relit la vente + lignes DANS une transaction `Serializable` (anti-TOCTOU), regroupe les lignes par `(productId, productVariantId)` et convertit via `convertToBase()` (`packages/utils`) si `saleUnitId` diffère de l'unité de stock du produit — un seul mouvement cumulé par couple produit/variante, jamais un appel par ligne brute — garde stock insuffisant (`BadRequestException`) avant tout appel à `ProductWarehouseService.adjustStock` (verrouillage optimiste sur `version`), catch `OptimisticLockException`/P2034 → `ConflictException` (409), `Sale.status` PENDING→COMPLETED, indépendant de `paymentStatus`/`PaymentSale` (S20). Émission `stock:updated`/`stock:lowAlert` + `NotificationService.createForOrg` après la transaction réussie. `SaleController` : route `PATCH /api/v1/sales/:id/validate`, permission `sales.validate` (ajoutée au seed, absente jusque-là), `@Auditable`. `SalesModule` étendu avec `InventoryModule`/`NotificationModule`. Écran : bouton "Valider la vente" (visible si `PENDING`) dans `SaleDetailView`, écoute Socket.io `stock:updated` (nouvelle dans `routes/sales/index.tsx`) invalidant `['sales']`/`['sale', id]`. Non-régression confirmée : `update()`/`remove()` rejettent toujours tout statut `!== PENDING`. Tests : 9 unitaires (dont regroupement multi-lignes et conversion d'unité 2 Cartons→24 Pièces) + 5 e2e (dont le **test de concurrence critère "Fait quand"** : stock=1, deux ventes, `Promise.all` sur `/validate` → exactement un 200 et un 409, stock final = 0) + 4 frontend. Discipline TDD respectée sur le test de concurrence : prouvé rouge (200/200 au lieu de 200/409) en abaissant temporairement l'isolation à `ReadCommitted`, puis vert après restauration de `Serializable` — modification non conservée dans le code final. `pnpm test`/`test:e2e` sur tout le monorepo : 100 % verts, aucune régression (API unitaires 448/448, API e2e 211/211 sur 21 suites, web 90/90). Lint + typecheck stricts verts. Revue sécurité dédiée (IDOR sur `validate()`, aucune décrémentation sous zéro même avec lignes dupliquées/conversion d'unité, non-régression update/remove, `@Auditable`) : aucun finding. |
| S21b | Annulation d'une vente validée : permission `sales.cancel`, restitution du stock sous verrouillage, statut `CANCELLED`, trace `AuditLog` (§18.18, §17 point AC) | Flow §18.18 complet ; le stock restitué est exact même en concurrence avec des ventes simultanées | ✅ 2026-07-27 — Migration `20260727110000_add_sale_cancellation` (`Sale.cancelReason`/`cancelledAt`/`cancelledById`, relation `SaleCancelledBy` nullable). `SaleService.cancel(id, organizationId, { reason, actorId })` : le regroupement/conversion d'unité et le mouvement de stock de `validate()` (S21) ont été extraits en helpers privés partagés (`groupDetailsByProduct`, `moveStock(direction: 'decrement'|'increment')`, `rethrowStockConflict`, `emitStockEvents`) pour ne dupliquer aucune logique Decimal sensible entre décrément et restitution. **Écart assumé par rapport au libellé strict de la session** : le guard accepte `COMPLETED` **et** `AWAITING_PAYMENT` (pas seulement `COMPLETED`) — nécessaire pour que le job d'expiration mobile money (S22) réutilise `cancel()` tel quel sur une vente jamais passée par `COMPLETED`, conformément à la raison explicite de fusion des deux sessions. Route `PATCH /api/v1/sales/:id/cancel` (`CancelSaleDto` zod, raison ≥ 3 caractères), permission `sales.cancel` (déjà en seed), `@Auditable`. Frontend : bouton "Annuler la vente" (visible si `COMPLETED`) + `CancelSaleDialog` (AlertDialog, raison obligatoire, patron standards.md). Tests : 9 unitaires `cancel()` (restitution simple/multi-lignes/conversion d'unité, rejet PENDING/CANCELLED, `AWAITING_PAYMENT` accepté, `ConflictException`) + 5 e2e (`AuditLog` acteur+raison, raison absente → 422, double annulation → 400, isolation tenant) + 5 frontend. |
| S22 | `PosModule` : `pos/calculTotal`, `pos/CreatePOS`, recherche produit/scan côté API | Flow §18.2 complet côté API (hors écran) | ✅ 2026-07-27 — Migration `20260727120000_add_webhook_event_sale_id` (`WebhookEvent.saleId`, parallèle à `invoiceId`). Nouveau `PosModule` : `GET /pos/products/search` (nom/code partiel, quantité stock LIVE par entrepôt via `ProductWarehouse`) ; `POST /pos/calculate-total` et la création réelle (`POST /pos/sales`) partagent la même fonction pure de calcul (`computeTotal`/`computeLineTotal`, aucun accès DB) — non-divergence testée explicitement ; `POST /pos/sales` fusionne en une seule transaction `Serializable` ce que S19/S21 séparent (création + décrément de stock), CASH/CARD encaissent immédiatement (`PaymentSaleService.createInTransaction`, nouvelle méthode extraite de `create()` pour être appelable dans une transaction déjà ouverte — réutilisée sans dupliquer le calcul `paidAmount`/`paymentStatus`), MOBILE_MONEY passe en `AWAITING_PAYMENT` (lien `PaymentAggregatorService.generatePaymentLink` généré **après** le commit, job différé `pos.expirePayment` enfilé sur la file dédiée `pos-payment-expiration`, délai `POS_PAYMENT_TIMEOUT_MS` par défaut 180000 ms). Webhook mobile money POS : **réutilisation du contrôleur existant** posé en T07 (`POST /webhooks/payments/:organizationId`) plutôt que la route `pos-payment` du plan de session — évite de dupliquer la vérification HMAC/l'idempotence déjà écrites et testées ; déplacé de `BillingModule` vers un nouveau `PosWebhookController` dans `PosModule` pour éliminer toute dépendance croisée `BillingModule ↔ PosModule` (`BillingModule` ne doit jamais hériter des dépendances transitives de `PosModule`). Worker dédié `PosPaymentExpirationWorker` (`apps/api/src/workers/`) : relit la vente, si toujours `AWAITING_PAYMENT` appelle `SaleService.cancel()` (S21b) avec `actorId: null`, journalise via `AuditService.create({ actorType: 'SYSTEM' })` — aucun contexte HTTP, `@Auditable` non applicable. Tests : 17 unitaires `pos.service.spec.ts` + 5 `pos-webhook.controller.spec.ts` + 5 `pos-payment-expiration.worker.spec.ts` + 11 e2e `pos.e2e.spec.ts` (dont le test de concurrence critère "Fait quand" sur le dernier exemplaire) + 3 e2e `pos-webhook.e2e.spec.ts` (confirmation, idempotence, expiration tardive). `pnpm lint`/`typecheck`/`test`/`test:e2e` : 100 % verts sur tout le monorepo (API unitaires 484/484, API e2e 230/230 sur 23 suites, web 95/95, database 10/10). |
| S23 | Écran web `pos.tsx` : panier, scan douchette USB (§17 point D), paiement, impression — y compris l'attente `AWAITING_PAYMENT` du mobile money (§18.2 étape 10) | ✅ 2026-07-28 — `routes/pos.tsx` : entrepôt obligatoire (§17 point J non tranché, pas de défaut), recherche produit debounced 300ms, grille cliquable avec avatar-initiales (décision actée : `GET /pos/products/search` de S22 ne renvoie pas d'image, `PosModule` non modifié), scan douchette (Entrée → recherche exacte hors debounce, ajout direct si correspondance unique sur `code`), panier avec vérification client du stock avant ajout (bloque + toast si dépassement, §18.2 étape 3), `POST /pos/calculate-total` debounced comme seule source du bandeau "Total" (jamais un total client pour la validation finale). Paiement CASH (monnaie calculée en direct, bloqué si insuffisant)/CARD (encaissement immédiat, reçu affiché, panier vidé, focus recherche) / MOBILE_MONEY (état d'attente, poll `GET /sales/:id` toutes les 3s jusqu'à `COMPLETED` ou `CANCELLED`, message d'expiration serveur affiché tel quel, "Réessayer"). Écoute Socket.io `stock:updated` (patron identique à `sales/index.tsx`/`transfers/index.tsx`). Reçu imprimable `components/PosReceipt.tsx` (nouvelle dépendance `react-to-print`, repli navigateur — impression ESC/POS thermique réelle hors périmètre, §17 point D). Nav + route câblées (`router.tsx`, `AppLayout.tsx`). Bug trouvé et corrigé en cours de session : `addProductToCart` lisait un flag de blocage de stock juste après `setCart(prev => ...)`, invalidé par le batching différé de React 18 (le toast ne partait jamais) — corrigé en lisant le panier du rendu courant avant l'appel. Tests : 15 Vitest (`routes/test/pos.spec.tsx` — recherche/debounce/vide/erreur, ajout clic et scan, calcul total, CASH avec monnaie, mobile money `COMPLETED`/`CANCELLED`, erreurs 400/409). `pnpm lint`/`typecheck`/`test` (web 110/110) et `pnpm test:unit` (API 484/484) verts sur tout le monorepo, aucune régression. Revue UX et revue sécurité dédiées (sous-agents) : sécurité sans finding ; UX 3 correctifs triviaux (chasse tabulaire) + 1 correctif de conception (état "aucun entrepôt sélectionné" : `ErrorState` trompeur → `EmptyState`, ce n'est pas une erreur). **Écart assumé par rapport au critère strict de la session** : le flow §18.2 a été vérifié de bout en bout via le contrat API réel (API + DB locales sur la branche courante — recherche, calcul de total, vente CASH, vente MOBILE_MONEY, erreur stock insuffisant, expiration simulée via `PATCH /sales/:id/cancel`) plutôt que manuellement dans un navigateur — aucun outil de navigation/capture d'écran disponible dans cet environnement d'exécution ; à confirmer visuellement par un humain avant merge. **Découverte hors périmètre, non corrigée** : le process worker BullMQ (`apps/api/src/worker.ts`) plante au démarrage (erreur de résolution NestJS, `DocumentCounterService` introuvable dans le contexte d'`InventoryModule` pour `AdjustmentService`), reproduit indépendamment de cette branche (conteneur Docker déjà en boucle de redémarrage avant cette session) — l'expiration automatique mobile money ne s'exécute donc pas réellement tant que ce bug pré-existant n'est pas corrigé ; `PosModule`/workers étant figés pour S23, signalé sans être corrigé. |
| S23b | Session de caisse : ouverture/clôture, fond de caisse, rattachement des ventes POS à la session, écart de caisse calculé à la clôture | Une journée de caisse s'ouvre, encaisse, se clôture avec un écart calculé et journalisé | ✅ 2026-07-29 — Décision §14 actée en début de session (recommandation ferme suivie). Migration `20260728090000_add_cash_session` : modèle `CashSession` (`openingAmount`/`expectedClosingAmount`/`countedClosingAmount`/`variance` en `Decimal(14,3)`, `status` `"OPEN"|"CLOSED"` en string simple), `Sale.cashSessionId` optionnel, `DocumentType.CASH_SESSION` (préfixe `CS`), index unique partiel `cash_sessions_org_user_open_key` (`organizationId`, `userId`) `WHERE status = 'OPEN'` — "chacun sa caisse". **Écart d'exécution signalé, pas tranché silencieusement** : l'historique de migrations existant était déjà cassé avant cette session (`20260727100000_add_payment_sale_fields` suppose `DocumentType`/`document_counters` créés hors migration, probablement via `prisma db push` antérieur) — `prisma migrate dev` impossible sur la shadow database ; migration écrite à la main et appliquée via `prisma migrate deploy`, vérifiée en base ; une migration de baseline reste à faire dans une session dédiée. `CashSessionModule` (`open`/`current`/`close`/`findAll`/`findOne`, permissions `cashsessions.view/open/close` en seed) + extension `PosService.createSale()` (gate session OPEN obligatoire, `cashSessionId` rattaché dans la même transaction Serializable que la vente — zéro TOCTOU avec une clôture concurrente). **Bug NestJS trouvé et corrigé** : `GET /cash-sessions/current` doit renvoyer `null` en JSON (jamais 204) quand aucune session n'est ouverte, mais `return null` depuis un contrôleur Nest est traité par le reply controller exactement comme `undefined` (`isNil`) et envoie un corps **vide**, pas le littéral `"null"` — cassait `res.json()` côté client (`SyntaxError` au lieu de résoudre `null`). Corrigé avec `@Res()` non-passthrough + `res.json(session)` explicite. **Gap IDOR trouvé et corrigé en revue sécurité dédiée** : `GET /cash-sessions/:id` ne vérifiait que l'isolation d'organisation, pas la propriété de la session — un caissier avec `cashsessions.view` (sans `records.viewAll`) pouvait consulter le détail (fond de caisse, écart) de la session d'un collègue ; corrigé avec le même patron `ViewAllInterceptor` que `findAll()`/`close()`. Frontend `routes/pos.tsx` étendu (pas réécrit) : gate bloquant (5 états) avant tout accès au panier, bandeau de session permanent (référence, heure d'ouverture, fond de caisse, bouton "Clôturer la caisse"), `AlertDialog` de clôture avec récapitulatif serveur (badge succès si excédent, danger si manque, jamais un nombre nu). Nouvel écran `routes/cash-sessions/index.tsx` (historique paginé + détail des ventes rattachées) + nav/router câblés. **Bug UX trouvé et corrigé lors du test manuel** : le sélecteur d'entrepôt avait été placé à l'intérieur du bloc conditionnel gaté par la session, le rendant invisible dès que le gate s'affichait — un choix d'entrepôt erroné devenait irréversible sans recharger la page ; sorti du bloc gaté, toujours visible. Tests : 20 unitaires `cash-session.service.spec.ts` (dont le gap IDOR de `findOne`) + 2 nouveaux `pos.service.spec.ts` (gate + rattachement, 10ᵉ argument constructeur `cashSessionService`) + 11 e2e `cash-session.e2e.spec.ts` (parcours complet ouverture→CASH+CARD→clôture avec `expectedClosingAmount` excluant CARD, IDOR sur close et sur le détail, isolation tenant) + extension `pos.e2e.spec.ts`/`pos-webhook.e2e.spec.ts` (session OPEN de fixture, sans quoi tous les tests POS existants échouaient désormais au gate) + 4 frontend (`pos.spec.tsx` — gate bloquant, ouverture, écart positif/négatif). `pnpm lint`/`typecheck`/`build` verts sur les 9 packages du monorepo (turbo, 20/20 tâches), `pnpm test`/`test:e2e` : API unitaires 506/506, API e2e 242/242 sur 24 suites, web 114/114, aucune régression. **Test manuel réel en navigateur** (Playwright piloté via un Chromium déjà mis en cache localement, `chromium-cli` indisponible dans cet environnement) sur des serveurs de dev lancés localement (Docker `api`/`worker` non utilisables : secrets `JWT_SECRET`/`APP_ENCRYPTION_KEY`/etc. absents de `docker-compose.yml`, incident distinct du worker déjà signalé en S23) : parcours complet ouverture (10 000 XAF) → vente CASH (1 000 XAF) → clôture (compté 10 800) → écart "Manque 200 XAF" affiché correctement, badge danger, historique à jour — captures d'écran à l'appui. |
| S24 | Écrans web ventes classiques (liste/création/détail) + envoi email/SMS | Flow §18.3 complet | ✅ 2026-07-30 — **Constat en début de session, périmètre réduit en conséquence** : liste/création/détail (`routes/sales/index.tsx`) étaient déjà entièrement construits par S19-S21b (historique des paiements, validation, annulation) — seul écart réel avec §18.3 étape 4 : l'envoi email/SMS d'une vente depuis l'écran de détail. Décision actée : périmètre limité à email-queue/sms-queue (Nodemailer/Twilio), sans pièce jointe PDF — `pdf-queue` (Puppeteer, templates brandés) reste une session séparée du plan P8. **Écart signalé et tranché avec le prompt de session** : `EmailService`/`SmsService` déplacés vers un nouveau module `apps/api/src/modules/messaging/` plutôt que `modules/notifications/` (ce dernier existe déjà pour les notifications persistantes in-app, S18 — sans rapport, un partage de nom aurait mélangé deux responsabilités distinctes). `MessagingModule` (`EmailService.sendSaleSummary` via Nodemailer + `SmtpServerService` par tenant existant, mode test si `NODE_ENV=test` — hors mode test, config absente → `BadRequestException` explicite plutôt qu'un faux succès silencieux ; `SmsService.sendSaleSummary` via Twilio, credentials plateforme `TWILIO_ACCOUNT_SID`/`TWILIO_AUTH_TOKEN`/`TWILIO_FROM_NUMBER`, pas de modèle `SmsServer` par tenant) + `MessagingQueueModule` (queues BullMQ `email`/`sms`, `attempts: 3` + backoff exponentiel, patron exact `PosPaymentExpirationQueueModule`) + `sale-message.renderer.ts` (rendu HTML/texte pur, échappement HTML testé contre injection). Deux workers dédiés `sale-email.worker.ts`/`sale-sms.worker.ts` déclarés dans `worker.module.ts` existant (aucun second process créé). `SaleService.send(id, organizationId, channel)` (nouvelle route `POST /api/v1/sales/:id/send`, permission `sales.view` réutilisée — action de partage sur une ressource déjà consultable, aucune nouvelle permission créée) : ownership vérifiée avant tout enfilage, `400` explicite si le client n'a pas l'email/téléphone requis, fire-and-forget (`202 Accepted`), aucune restriction de statut de vente. `CLIENT_WAREHOUSE_INCLUDE` étendu avec `client.email`/`client.phone` (additif, sans régression sur les usages existants). Frontend : boutons "Envoyer par email"/"Envoyer par SMS" dans `SaleDetailView` (icônes Mail/MessageSquare, tooltip Radix déjà présent au repo réutilisé pour l'état désactivé, toast "Envoi en cours…" — jamais une confirmation de livraison qu'on n'a pas). Revue sécurité dédiée (sous-agent) : aucune fuite de secret SMTP/Twilio dans les logs (vérifié ligne à ligne), IDOR vérifiée à la fois côté route et côté worker (organizationId revérifié à chaque rechargement), injection d'en-tête email analysée au niveau du code source Nodemailer (neutralisation CRLF confirmée) + XSS testé sur le rendu HTML (`<script>`/`onerror` échappés) — aucun correctif nécessaire ; observation non bloquante : pas de rate limiting dédié sur cette route (hors du périmètre strict CLAUDE.md qui ne l'exige que pour login/reset/endpoints publics), à envisager si abus constaté. Tests : `messaging`/`workers` 37 unitaires + `sale.service.spec.ts` 7 nouveaux (54/54 au total) + `sale.e2e.spec.ts` 8 nouveaux (41/41 au total, dont isolation tenant et `AuditLog` `sales.send` vérifié en base) + frontend typecheck/lint verts. `pnpm lint`/`typecheck` (API) verts, `pnpm test:unit` (API) 54 suites/538 tests verts, `pnpm test:e2e` (API) 24 suites/250 tests verts, `pnpm --filter web typecheck`/`lint` verts — aucune régression. |

### Bloc F (= P6) — Achats & retours (dépend du Bloc E pour les retours de vente)

| # | Contenu | Fait quand… |
|---|---|---|
| S25 | `Purchase` + `PurchaseDetail` + `PaymentPurchase`, incrément de stock à la validation | Flow §18.7 complet | ✅ 2026-08-02 — Mirror exact du domaine Vente (S19+S20+S21 condensées en une session), en sens inverse. Schéma : `Purchase`/`PurchaseDetail`/`PaymentPurchase` (migration manuelle `20260802100000_add_purchase` — `prisma migrate dev` cassé sur la shadow database, dette pré-existante déjà documentée en S23b ; appliquée via `prisma migrate deploy`), `DocumentType.PAYMENT_PURCHASE` (préfixe `PAA`), permission manquante `purchases.validate` ajoutée au seed (`paymentPurchases.*` existaient déjà). **Écart architectural assumé et validé avec l'utilisateur avant codage**, sur deux points où « mirror exact » aurait cassé le parcours métier ou l'attente de test : (1) permissions de paiement **dédiées** `paymentPurchases.*` plutôt que réutiliser `purchases.*` — même précédent que `paymentSales.*` pour les ventes ; (2) `validate()` **crée le `ProductWarehouse` s'il est absent** (dans la même transaction Serializable, quantity=0/version=0 puis incrément immédiat) plutôt que de lever une erreur bloquante comme `SaleService.moveStock` — la première réception d'un produit dans un entrepôt EST ce qui crée son stock, contrairement à une vente qui exige un stock préexistant. **Retry automatique borné (5 tentatives)** ajouté dans `PurchaseService.validate()` sur conflit de concurrence (`OptimisticLockException`/P2034) — absent de `SaleService.validate()`, décision actée explicitement avec l'utilisateur : un incrément est toujours mécaniquement fusionnable (commutatif), contrairement à un décrément où l'échec peut porter une vraie information métier (stock désormais insuffisant) qu'un retry aveugle masquerait ; sans ce retry, le test de concurrence "lost update" (deux incréments simultanés sur le même `ProductWarehouse`) aurait produit un seul gagnant (200/409) comme en S21, au lieu des deux succès attendus avec quantité finale exacte. Pas de garde stock insuffisant (n'a pas de sens à l'incrément), pas de `stock:lowAlert` (n'a de sens qu'à la baisse). **Aucun `cancel()`** dans cette session : `purchases.cancel` existe en permission depuis le début du plan mais aucune session ne l'implémente encore — signalé explicitement plutôt que tranché silencieusement, à couvrir dans une session dédiée future (mirror S21b pour Sale). Revue sécurité dédiée (sous-agent) : IDOR vérifiée dans la transaction sur `provider`/`warehouse`/`produit`/paiement, immutabilité `COMPLETED` confirmée y compris à travers les tentatives de retry, **un trou de cohérence réel trouvé et corrigé** — une `Unit` d'achat mal configurée (`operatorValue: 0`) pouvait produire un incrément de stock nul ou `Infinity` après conversion sans qu'aucune garde n'existe ; garde ajoutée dans `moveStock` (`BadRequestException` si la quantité convertie n'est pas finie et strictement positive) ; aucun appel à `purchases.cancel` nulle part (front/back), pas de nouveau `$queryRaw`. Deux observations non corrigées car pré-existantes et hors périmètre : TOCTOU mineur sur `update()`/`remove()` (statut lu hors transaction) — identique et déjà accepté sur `Sale` ; RLS PostgreSQL non étendue à `purchases`/`sales`/`products` (seule `users`/`roles` l'ont) — état global du projet, pas une régression de cette session. Frontend `routes/purchases/index.tsx` — **livré dans cette session plutôt qu'en S27** (condensation actée, cf. note sous S27) : liste/filtres, création, détail avec historique paiements, bouton « Valider l'achat », **pas** de bouton d'annulation (cohérent avec l'absence de `cancel()`), **pas** d'envoi email/SMS (spécifique à S24/Sale, hors périmètre) ; nav « Achats » sous la section renommée « Ventes & Achats », icône `PackagePlus`. Tests : 43 unitaires (`purchase.service.spec.ts` 27 dont retry et garde de conversion, `payment-purchase.service.spec.ts` 16) + 21 e2e `purchase.e2e.spec.ts` (CRUD, validate, conversion d'unité, création auto du `ProductWarehouse`, isolation tenant, **test de concurrence "lost update" — deux achats validés simultanément sur le même `ProductWarehouse`, les deux réussissent, quantité finale = somme exacte**, rejoué 5 fois sans flakiness) + 15 frontend (`purchases.spec.tsx`). `pnpm lint`/`typecheck` verts sur les 9 packages, `pnpm test:unit`+`test:integration` (API 549/549, database 10/10, utils 16/16) et `pnpm test:e2e` (API 263/263 sur 25 suites) et web (134/134) verts, aucune régression. **Note** : branche basée directement sur `origin/main` (S24 non encore mergée au moment de cette session — indépendant fonctionnellement, aucun conflit attendu). |
| S26 | `SaleReturn` + `PurchaseReturn` (+détails+paiements), ajustement de stock inverse | Flow §18.6 complet dans les deux sens | ✅ 2026-08-02 — Backend uniquement (comme S22 pour le POS) : l'écran web reste explicitement différé à S27, aucun écran construit ici. Schéma : `SaleReturn`/`SaleReturnDetail`, `PurchaseReturn`/`PurchaseReturnDetail` (mirror Sale/Purchase, sans `isPos`/`cashSessionId`/annulation), `PaymentReturn` **unique et partagé** entre les deux sens avec deux FK nullables (`saleReturnId`/`purchaseReturnId`) en parallèle exact du patron `WebhookEvent.saleId/invoiceId` — indice du plan confirmé juste, pas deux modèles séparés. `DocumentType.PAYMENT_RETURN` (préfixe `REM`, validé avec l'utilisateur), migration manuelle `20260802150000_add_returns` (même dette shadow-database que S23b/S25, appliquée via `prisma migrate deploy`), **contrainte `CHECK` SQL `payment_returns_exactly_one_parent_chk` (`num_nonnulls` = 1)** posée en base en plus de la validation applicative — défense en profondeur vérifiée par insertion SQL brute réelle (deux FK non-null puis deux FK null, les deux rejetées par Postgres). Permissions `saleReturns.validate`/`purchaseReturns.validate` ajoutées au seed (manquantes, `paymentReturns.*` déjà présent). **Décision de conception actée avant codage** : le client ne fournit jamais price/taxAmount/taxMethod/discount/discountMethod ni `warehouseId` sur une ligne de retour — toujours copiés depuis la `SaleDetail`/`PurchaseDetail` source (anti-manipulation de prix), seuls `saleId`/`purchaseId`, la ligne source et la quantité retournée sont acceptés côté DTO ; le discriminant de `PaymentReturn` est déterminé exclusivement par la route appelée (`createForSaleReturn`/`createForPurchaseReturn`), jamais par le corps de la requête. `validate()` mirror l'asymétrie retry déjà actée en S25 : `SaleReturn` (incrément, commutatif) retente automatiquement comme `Purchase.validate()` ; `PurchaseReturn` (décrément) ne retente jamais comme `Sale.validate()`, même raisonnement métier appliqué en miroir. **Le point le plus subtil de la session** : le plafond de quantité retournable est vérifié en **unité de base du produit** (conversion via `convertToBase`), pas sur les quantités brutes — une ligne vendue en carton peut être retournée en pièces sans que la comparaison ne se fasse à tort sur des unités différentes ; testé explicitement (cas qui échouerait silencieusement sans la conversion). Garde en deux niveaux : `create()` fait une vérification best-effort par ligne (ignore les autres retours existants) ; `validate()` fait la garde **autoritative cumulée** (somme tous les retours `COMPLETED` existants sur la même ligne source + cette ligne, comparée à la quantité d'origine) **dans la transaction Serializable** qui mouvemente le stock — c'est l'isolation Postgres (SSI, write skew) qui empêche deux validations concurrentes de dépasser ensemble le plafond, pas un verrou applicatif. Répartition en 6 agents experts comme prescrit par le plan de session (migration bloquante, 3 domaines en parallèle une fois le schéma livré, tests, revue sécurité en gate final) ; le typecheck API est resté vert après intégration des 3 domaines développés en parallèle, sans conflit. **Revue sécurité dédiée (gate final) : un vrai bug trouvé et corrigé, mais pas dans le code de production** — le fichier de test `sale-return.e2e.spec.ts` référençait une variable jamais déclarée (`pwValBaseline`), une erreur de compilation TypeScript qui empêchait la suite (18 tests) de s'exécuter du tout, invalidant silencieusement un rapport antérieur de « 107/107 tests verts » ; corrigé, et le scénario de contournement par fractionnement séquentiel qu'il visait (5 retours successifs de 3 sur une ligne source de 10 : les 3 premiers réussissent cumulativement jusqu'à 9/10, le 4ᵉ doit échouer sans troncature silencieuse) a aussi été ajouté côté `PurchaseReturn` où il manquait. IDOR croisé confirmé bloqué à trois niveaux (autre document même organisation, autre organisation, `create()`/`update()` partagent la même vérification `saleDetailId`/`purchaseDetailId` → document déclaré, pas seulement organisation). Tests : 55 unitaires (3 services) + 54 e2e (3 fichiers, dont les deux tests de concurrence write-skew — `SaleReturn` déterministe `[200, 400]` sur 9 exécutions observées car le retry relit l'état et découvre la violation métier, `PurchaseReturn` déterministe `[200, 409]` mirror exact du patron S21, invariants de stock/quantité cumulée vérifiés en base après chaque `Promise.all`) — 109/109 après correction. `pnpm lint`/`typecheck` verts sur les packages touchés (`api`, `database`, `utils`), `pnpm test:unit` API 604/604 (54 suites) et `pnpm test:e2e` API 317/317 (28 suites) sur l'ensemble du dépôt, aucune régression. |
| S27 | Écrans web achats + retours | Parcours testés à l'écran | ✅ 2026-08-06 — Écran achats déjà livré en S25 (condensé avec le backend plutôt que reporté ici, confirmé au démarrage) : cette session n'a couvert QUE les écrans retours (`SaleReturn`/`PurchaseReturn`), backend S26 mergé sur main au préalable (mergé manuellement — PR ouverte par l'utilisateur via l'UI GitHub, `gh` non authentifié dans cet environnement). Deux nouveaux écrans (`routes/sale-returns/index.tsx`, `routes/purchase-returns/index.tsx`, mirror strict de `sales/index.tsx`/`purchases/index.tsx`) + un point d'entrée « Créer un retour » sur les écrans Ventes et Achats existants (visible uniquement si `status === 'COMPLETED'`, ouvre un Sheet listant les lignes du document d'origine avec quantité à retourner par ligne, plafonnée côté client au restant retournable — calculé en sommant les retours `COMPLETED` déjà existants sur chaque ligne source, le serveur restant l'arbitre final). **Décision de portée actée avant codage** : ni `SaleForm` ni `PurchaseForm` existants ne proposent de sélecteur d'unité (`saleUnitId`/`purchaseUnitId` toujours omis) — le formulaire de création de retour suit la même limite, aucun sélecteur `returnUnitId`, cohérent avec le niveau de support déjà en place plutôt qu'une extension de portée non demandée. **Mécanisme de deep-link après création** (choix explicite, discuté avec l'utilisateur) : après un `POST` réussi, navigation vers `/sale-returns?open=<id>` (ou `/purchase-returns`) pour ouvrir automatiquement le détail du retour créé — implémenté SANS le `validateSearch` typé de TanStack Router (aucune route du projet ne l'utilisait avant cette session, et l'introduire aurait cassé le patron de test existant où chaque page est montée directement sans `RouterProvider`) : lecture via `new URLSearchParams(window.location.search).get('open')` en initialisation paresseuse d'état, navigation via `useNavigate({ search: { open } })` en pass-through non typé — mécanisme cohérent des deux côtés (vente/achat), vérifié identique en revue finale. Aucun champ price/taxAmount/discount n'est éditable côté client sur une ligne de retour (lecture seule sur les écrans de détail, uniquement une quantité sur le Sheet de création) — vérifié explicitement en revue UX/sécurité dédiée. Pas de bouton « Annuler » sur un retour (endpoint inexistant, cohérent avec l'absence de S26b). Navigation : deux entrées « Retours de vente »/« Retours d'achat » ajoutées dans la section sidebar « Ventes & Achats » (icône `Undo2`). **Un vrai bug trouvé et corrigé en revue finale** : le badge de statut de paiement des deux nouveaux écrans avait été copié tel quel depuis `sales/index.tsx` sans adapter le vocabulaire — affichait « Non payé »/« Payé » alors que la colonne s'appelle « Remboursement » ; corrigé en « Non remboursé »/« Remboursé ». Répartition en agents parallèles comme prescrit par le plan de session (écrans vente/achat en parallèle, navigation, revue finale) ; plusieurs relances nécessaires suite à des interruptions techniques (stalls réseau/outil) pendant la session — travail partiel de chaque tentative interrompue vérifié et repris plutôt que jeté, sans perte ni duplication. **Test bout en bout (le « Fait quand » de cette session)** : l'outil de pilotage de navigateur (Playwright MCP) était déconnecté dans cet environnement — aucun test visuel réel effectué (le rendu DOM des Sheets/toasts n'a jamais été vu dans un navigateur, signalé explicitement plutôt que prétendu). À la place : (1) suites e2e backend S26 relancées contre le vrai Postgres/Redis local, 54/54 vertes ; (2) test manuel API réel (serveur `ts-node` de dev, toutes routes mappées) reproduisant exactement le contrat consommé par les écrans — sens vente (stock 50→40 à la vente, retour partiel qty 3 validé → stock 43, remboursement 4500 XAF → `PAID`) et sens achat (stock 43→63 à l'achat, retour partiel qty 5 validé → stock 58, remboursement 5000 XAF → `PAID`), les deux sens vérifiés avec les mêmes requêtes que le frontend (`GET` filtrés par `warehouseId`/`status` inclus). `pnpm lint`/`typecheck` verts sur les 9 packages (turbo), `pnpm --filter @ensemb/web test` : 156/156 (16 suites), aucune régression. |

### Bloc G (= P7) — Devis, dépenses (dépend du Bloc E)

| # | Contenu | Fait quand… |
|---|---|---|
| S28 | `Quotation` + `QuotationDetail` + conversion en vente (`Change_to_Sale`) | Flow §18.4 complet | ✅ 2026-08-09 — Backend uniquement (comme S22/S26) : l'écran web reste explicitement différé à S30, aucun écran construit ici. Schéma : `Quotation`/`QuotationDetail` (mirror `Sale`/`SaleDetail`, **sans** `paymentStatus`/`paidAmount`/`isPos`/`cashSessionId`/champs d'annulation — un devis n'est jamais payé et ne s'annule pas dans cette session ; `quoteUnitId` au lieu de `saleUnitId`), `Sale.quotationId` (`String? @unique`, migration manuelle `20260803090000_add_quotation`, même dette shadow-database que S23b/S25/S26, appliquée via `prisma migrate deploy`, pas de RLS ajoutée — cohérent avec `sales`/`purchases`/`returns` qui n'en ont pas non plus). Permission `quotations.convert` ajoutée au seed. `QuotationsModule` (create/findAll/findOne/update/remove/send/convert) : aucun mouvement de stock à aucun moment (ni création, ni update/remove, ni conversion — seule la validation ultérieure de la `Sale` résultante, via la route S21 existante et non modifiée, mouvemente le stock) ; `send()` mirror exact S24 (permission `quotations.view` réutilisée, pas de permission dédiée) ; `convert()` — transaction Prisma standard (pas Serializable, aucun stock en jeu), relit le devis dans la transaction (anti-TOCTOU), rejette si non-`PENDING`, crée la `Sale` **directement via `tx.sale.create`** (pas d'appel à `SaleService`, frontière de module déjà établie par `PosService`) avec référence issue de **la séquence `DocumentType.SALE`** (pas une séquence `DEV` dédiée — point explicite du plan, vérifié par test), lignes copiées 1:1 (`quoteUnitId → saleUnitId`, aucun recalcul), devis passé à `COMPLETED`, émission `sale:created` (pas `stock:updated`). Messaging : `EmailService.sendQuotationSummary`/`SmsService.sendQuotationSummary` (mirror exact `sendSaleSummary`), `quotation-message.renderer.ts` (libellés de statut de devis plutôt que `paymentStatus`, inexistant ici), workers `quotation-email.worker.ts`/`quotation-sms.worker.ts` enregistrés dans `WorkerModule`. Répartition en 5 agents experts comme prescrit par le plan de session (migration bloquante, backend+messaging en parallèle, tests, revue sécurité en gate final). **Revue sécurité dédiée (gate final) : un vrai problème trouvé et corrigé** — `convert()` n'attrapait pas la violation de contrainte unique Postgres (`Sale.quotationId`) en cas de double conversion concurrente sur le même devis (deux requêtes lisant toutes deux `PENDING` sous l'isolation READ COMMITTED avant qu'aucune ne commite) : reproduit empiriquement (un 201, un 500 générique), corrigé par un `catch` dédié (`rethrowConversionConflict`, mirror `SaleService.rethrowStockConflict`) traduisant `PrismaClientKnownRequestError` P2002 en `ConflictException` (409, message actionnable) ; pas de fuite de détail interne dans les deux cas (aucun `ExceptionFilter` custom, le filtre NestJS par défaut renvoyait déjà un message générique), mais un défaut de robustesse réel désormais couvert par un test de concurrence dédié (`Promise.all`, exactement un 201 + un 409, une seule `Sale` créée en base). IDOR confirmée sur `clientId`/`warehouseId`/lignes (vérifiés dans la transaction à `create()`/`update()` ; `convert()` ne recopie que des données déjà validées en base, aucun `@Body()` sur cette route) ; aucun couplage `InventoryModule`/`ProductWarehouseService` dans `QuotationsModule` ; aucune route cancel/reopen créée par erreur ; DTOs zod stricts, aucun mass assignment. Tests : 20 unitaires (`quotation.service.spec.ts`) + 27 e2e (`quotation.e2e.spec.ts` : CRUD, IDOR, `records.viewAll`, `convert()` nominal/double-conversion séquentielle et concurrente/404/IDOR/permission manquante, `send()`, **test bout en bout devis → conversion → `PATCH /sales/:id/validate` (route S21 existante, non modifiée) → stock décrémenté** prouvant l'absence totale de traitement spécial dans `SaleService` pour une vente issue d'une conversion) + extensions `email.service.spec.ts`/`sms.service.spec.ts`/nouveaux specs workers/renderer. `pnpm lint`/`typecheck` verts sur les 9 packages (turbo), `pnpm test:unit` API 686/686 (63 suites) et `pnpm test:e2e` API 352/352, aucune régression. |
| S29 | `ExpenseCategory` + `Expense` | CRUD simple fonctionnel | ✅ 2026-08-09 — Backend uniquement (comme S28) : l'écran web reste explicitement différé à S30. Session volontairement simple, pas sur-architecturée : `Expense` est un fait comptable unique et immédiat, PAS un document commercial — aucun statut PENDING/COMPLETED, aucun mouvement de stock, aucun paiement partiel, aucune transaction Serializable ni verrouillage optimiste. Schéma : `ExpenseCategory` (mirror `Brand`/`Category` — donnée de référence simple, `userId` obligatoire, soft delete, index unique partiel `unique_expense_category_name_active` sur `(organizationId, name) WHERE deletedAt IS NULL`) et `Expense` (mirror léger `Sale` côté en-tête uniquement — pas de lignes de détail, entité plate — `reference` générée via `DocumentCounterService`/`DocumentType.EXPENSE`, préfixe `DEP` ajouté à `packages/utils/src/document-reference.ts`, `amount Decimal(14,3)` obligatoire, `details String` **obligatoire** — corrigé après une première tentative erronée en `String?` par l'agent migration, écart signalé et corrigé avant de lancer le backend). Migration manuelle `20260804090000_add_expense`, même dette shadow-database que S23b/S25-S28, appliquée via `prisma migrate deploy`, pas de RLS ajoutée (cohérent avec le reste). Permissions `expenseCategories.view/create/edit/delete` ajoutées au seed (`expenses.*` existaient déjà). `ExpensesModule` : `ExpenseCategoryService`/`Controller` (`@Auditable` sur create/update/delete, mirror exact `BrandService` y compris `P2002`→`ConflictException`) et `ExpenseService`/`Controller` (`create()`/`update()` vérifient `expenseCategoryId`/`warehouseId` dans la transaction — anti-IDOR/TOCTOU ; `update()` sans restriction de statut, il n'y en a pas ; `remove()` soft delete uniquement ; `@Auditable` sur update/remove seulement, pas create — convention documentaire S19/S24/S28 ; `records.viewAll`/`ViewAllInterceptor` sur la liste). Ni `RealtimeModule` ni `MessagingQueueModule` importés (aucun événement temps réel ni envoi email/SMS pour une dépense dans cette session — périmètre volontairement restreint). **Incident détecté et résolu en cours de session (à retenir pour les sessions futures)** : la correction du type de `Expense.details` (`String?` → `String`) a nécessité une reconstruction de la base de dev locale (`prisma migrate reset` a révélé, en plus de la dette shadow-database déjà connue, que le replay complet de l'historique échoue réellement sur la vraie base — pas seulement la shadow DB comme documenté jusqu'ici : `DocumentType` est référencé par la migration `20260727100000_add_payment_sale_fields` avant d'être créé par `20260728090000_add_cash_session`, bug d'ordonnancement réel dans l'historique de migrations). Contournement appliqué : `prisma db push --force-reset` (resynchronise le schéma directement, ignore l'historique) puis ré-application manuelle de **tout le SQL brut non représentable dans `schema.prisma`** — invisible à `db push` — retrouvé par audit exhaustif plutôt que par supposition : les 2 politiques RLS + rôle `ensemb_app` (`users`/`roles`), la contrainte `CHECK payment_returns_exactly_one_parent_chk`, et **12 index uniques partiels** `WHERE deletedAt IS NULL` (`warehouses`, `categories` ×2, `brands`, `units` ×2, `clients`, `providers`, `products`, `product_warehouse` ×2 — celui-ci protège l'anti-survente §17 point B, le plus critique des douze — `cash_sessions` `WHERE status = 'OPE­N'`, `expense_categories`) ; tous les 30 migrations ensuite marquées `applied` via `prisma migrate resolve`. Un test e2e existant (`warehouse.e2e.spec.ts`, contrainte de nom actif unique) a détecté la première régression (409 attendu, 201 reçu), ce qui a déclenché l'audit exhaustif plutôt qu'une correction ponctuelle — toute la suite e2e complète relancée après coup pour confirmer l'absence d'autre trou. La dette de migration elle-même (bug d'ordonnancement `DocumentType`) n'a pas été corrigée dans l'historique existant — hors périmètre de cette session, mais l'ampleur réelle du problème (réplique cassée même hors shadow DB) est documentée ici pour la première fois avec précision. Revue sécurité dédiée (sous-agent) : aucun finding — référence via `DocumentCounterService` confirmée (jamais `MAX()+1`), IDOR vérifiée dans la transaction sur `expenseCategoryId`/`warehouseId` (create et update), soft delete confirmé partout (aucun `delete()`/`deleteMany()`), pas de mass assignment, permissions et guards cohérents, `Decimal` de bout en bout, pas de fuite inter-organisation dans les réponses de liste. Tests : 38 unitaires (`expense-category.service.spec.ts` 17, `expense.service.spec.ts` 21) + 23 e2e (`expense.e2e.spec.ts` : CRUD des deux entités, isolation tenant, référence `DEP-YYYY-NNNNNN` séquentielle unique par organisation, IDOR sur `expenseCategoryId`/`warehouseId` d'une autre organisation en création et modification, **suppression d'une `ExpenseCategory` encore référencée par une `Expense` existante** — la catégorie disparaît des listes, la dépense reste consultable avec son `expenseCategoryId` intact). `pnpm lint`/`typecheck` verts sur les 9 packages (turbo), `pnpm test:unit` 724/724 (65 suites) et `pnpm test:e2e` 375/375 (30 suites), aucune régression après restauration des index. |
| S30 | Écrans web devis + dépenses | Parcours testés à l'écran | ✅ 2026-08-13 — Trois écrans créés : `routes/quotations/index.tsx` (mirror `sales/index.tsx` sans section paiements/annulation/retours — un devis n'est jamais payé), `routes/settings/expense-categories.tsx` (mirror strict `brands.tsx` sans champ image) et `routes/expenses/index.tsx` (mirror simplifié `purchases/index.tsx` — entité plate, pas de lignes de détail, pas de statut de cycle de vie, édition inline via Sheet réutilisé plutôt qu'une vue détail séparée). `QuotationStatusBadge` avec libellés dédiés au devis (`PENDING` → « Brouillon », `COMPLETED` → « Converti », `CANCELLED` → « Annulé »), volontairement distincts de `StatusBadge` (ventes) — vérifié explicitement en test qu'aucun libellé de vente (« En attente »/« Terminée ») ne fuite sur cet écran. **Écart assumé sur la redirection post-conversion, signalé et validé avec l'utilisateur avant codage** : le prompt de session demandait une redirection vers `/sales/:id`, route qui n'existe pas dans `router.tsx` (le détail d'une vente s'ouvre via un Sheet piloté par état local, pas une route par ID) — recherche web menée sur les bonnes pratiques UX (redirection automatique vers le document converti confirmée comme attendu par Xero/Zoho/InvoiceNinja ; encodage d'état via query param confirmé comme un patron SPA reconnu) puis application du patron déjà établi en S27 pour ce cas exact : deep-link `?open=<id>` lu une seule fois au montage (`new URLSearchParams(window.location.search).get('open')`, pas de `validateSearch` TanStack Router), `SalesPage` modifiée pour le lire. Bouton « Convertir en vente » : `AlertDialog` nommant le devis par référence, **sans** raison obligatoire (contrairement à l'annulation de vente S21b — la conversion n'est pas destructive, elle produit un nouveau document), désactivé si `status !== 'PENDING'`. Boutons d'envoi email/SMS : mirror exact S24, désactivés en plus si le devis n'est plus `PENDING` (contrainte propre au devis, absente de `SaleDetailView`). Navigation : « Devis » ajouté à la section « Ventes & Achats », nouvelle section « Finances » avec « Dépenses », « Catégories de dépenses » dans « Paramètres ». Répartition en agents parallèles comme prescrit par le plan de session (devis / dépenses+catégories frontend, tests, revue finale) ; **les deux premières tentatives de délégation des tests à des agents en arrière-plan ont calé sans laisser de fichier** (`stream watchdog` — timeout de 600s sans progrès, cause non identifiée côté outillage) — signalé plutôt que masqué, tests finalement écrits directement plutôt que retentés une troisième fois. Deux écarts pré-existants découverts et signalés sans être corrigés (hors périmètre de cette session) : `apps/web/package.json` expose un script `test` (vitest) et non `test:unit`, si bien que le pipeline `turbo test:unit` ne couvre jamais les tests web (contrairement à `apps/api` qui utilise bien `test:unit`) — la suite web a donc été vérifiée directement via `vitest run` plutôt que via `turbo` ; le conteneur Docker `ensemb-worker-1` tourne en `Restarting` en boucle depuis l'observation initiale (`docker ps`), non lié aux changements de cette session. Tests : 39 nouveaux (`quotations.spec.tsx` 18 — 5 états, libellés de statut dédiés, création, boutons d'envoi actifs/désactivés selon statut et coordonnées client, conversion avec appel API + toast vérifiés explicitement, suppression conditionnelle au statut ; `expense-categories.spec.tsx` 8 — CRUD complet mirror `brands.spec.tsx` ; `expenses.spec.tsx` 13 — CRUD complet, filtres catégorie/entrepôt/date, résolution des noms côté client). `pnpm turbo lint`/`typecheck --filter=@ensemb/web` verts, suite complète `vitest run` du package web 195/195 (19 fichiers), aucune régression. **Test manuel de bout en bout réel effectué** (API locale `ts-node` + Vite dev server contre Postgres/Redis Docker existants, piloté au navigateur via Playwright MCP, identifiants seed `admin@demo.ensemb.cm`/sous-domaine `demo`) : création d'entrepôt/catégorie/produit/client de test (organisation démo vierge), devis DEV-2026-000001 créé (2× produit, 30 000 XAF), envoi email confirmé par la requête réseau réelle (`POST /quotations/:id/send` → 202), conversion confirmée par `AlertDialog` nommant le devis puis redirection réelle observée vers `/sales?open=<id>` avec le détail de la vente VTE-2026-000001 pré-ouvert (mêmes lignes, même total) et le devis d'origine repassé à « Converti » en liste ; côté dépenses, catégorie « Électricité » créée puis dépense DEP-2026-000001 créée dessus, apparaissant en liste avec catégorie et entrepôt résolus par leur nom (pas par UUID). |
| S30b | Premier dry-run de la migration MySQL → PostgreSQL sur un extrait de données réelles (schéma documentaire stabilisé — §13 P7) | Extraction/chargement rejouables, écarts documentés ; S50 devient l'exécution finale d'un script déjà éprouvé |

### Bloc H (= P8) — Paiements & intégrations externes (dépend des Blocs E/F/G)

| # | Contenu | Fait quand… |
|---|---|---|
| S31 | Intégration de l'agrégateur de paiement (`PaymentWithCreditCard` : carte, Orange Money, MTN MoMo) sur POS et ventes classiques, identifiants propres à chaque organisation (chiffrés, §17 point S) ; **flux asynchrone** : `AWAITING_PAYMENT`, webhook idempotent, expiration configurable avec restitution du stock (§17 point V) | Un paiement carte ou mobile money test aboutit, mapping local enregistré ; un paiement non confirmé expire et restitue le stock | ✅ 2026-08-23 — Backend uniquement. **Décision de conception actée avant codage (point bloquant du prompt)** : le flux `AWAITING_PAYMENT` + restitution de stock du plan concerne exclusivement le POS (stock décrémenté à la création, S22) — une vente classique découple totalement l'encaissement (S20) de `validate()` (S21, seule étape qui mouvemente le stock), donc un paiement en ligne différé n'a rien à restituer et ne doit JAMAIS toucher `Sale.status` (immutabilité `COMPLETED`, règle 7). Nouveau modèle séparé `OnlinePaymentIntent` (`PENDING/CONFIRMED/EXPIRED`) porte seul cette attente pour la vente classique ; `PaymentSale` n'est créé qu'à la confirmation webhook, l'expiration ne fait que passer l'intention à `EXPIRED` sans aucun autre effet de bord. Second point tranché dans le plan : `PaymentGatewayCredential` est **unique par organisation** (pas de champ `provider`) — un agrégateur type CinetPay/Monetbil route CARD/Orange Money/MTN MoMo via un seul compte marchand, le provider réel n'est connu qu'au retour du webhook (`PaymentWithCreditCard.provider`, jamais choisi à l'initiation) ; permission réutilisée `organization.settings.edit` (pas de nouvelle permission, mirror strict `SmtpServerService`/`SmtpServerController`, T05). Schéma : `PaymentGatewayCredential` (`apiKeyCipher`/`webhookSecretCipher` chiffrés AES-256-GCM via `EncryptionService`, jamais en clair en réponse HTTP), `OnlinePaymentIntent`, `PaymentWithCreditCard` (squelette 01-architecture.md), `WebhookEvent.onlinePaymentIntentId` (FK nullable en parallèle de `saleId`/`invoiceId`, même patron S22/T07) — deux migrations manuelles (`20260805090000_add_payment_gateway_credential`, `20260806090000_add_online_payment`, générées via `prisma migrate diff` puis appliquées par `prisma migrate deploy`, même dette shadow-database que S23b+). POS : `CARD` bascule sur le même flux asynchrone que `MOBILE_MONEY` (une vraie transaction carte n'est jamais instantanée) — `PosService.confirmMobileMoneyPayment` généralisée en `confirmAsyncPayment(organizationId, saleId, confirmation)`, méthode d'encaissement dérivée du provider rapporté par le webhook (jamais du choix initial du client), `PaymentWithCreditCard` créé à la confirmation pour POS et vente classique. `TenantPaymentGatewayService` (résolution credentials par org, mode test si absent) et `AsyncPaymentService` (génération de lien + persistance/idempotence `WebhookEvent` partagées) extraits de l'ancien `PosWebhookController`/`PaymentAggregatorService` ; webhook généralisé unique `PaymentsWebhookController` (`POST /api/v1/webhooks/payments/:organizationId`, nouveau module dédié `PaymentsWebhookModule` impotant `PosModule`+`SalesModule`+`PaymentGatewayModule` pour éviter tout cycle), routage sur `saleId` (POS) vs `intentId` (vente classique) mutuellement exclusifs. Répartition en 6 agents experts comme prescrit par le plan de session (schéma/credentials chiffrés bloquant, service partagé, POS/vente classique en parallèle une fois le schéma OnlinePaymentIntent/PaymentWithCreditCard posé séquentiellement pour éviter un conflit d'édition sur `schema.prisma`, tests, revue sécurité en gate final) ; glue finale (généralisation du contrôleur webhook, retrait de `BillingModule` de `PosModule`) faite directement plutôt que déléguée, point d'intégration touchant les livrables des deux agents parallèles. **Deux vrais bugs trouvés et corrigés en gate final :** (1) *découvert par les tests de compétition webhook/expiration ajoutés pour la nouvelle variante CARD* — `SaleService.cancel()` acceptait à la fois `COMPLETED` (annulation manuelle S21b) et `AWAITING_PAYMENT` (expiration POS S22), donc un webhook de confirmation gagnant la course contre le job d'expiration retardé se faisait écraser : la vente déjà payée repassait `CANCELLED` et son stock était restitué une seconde fois — corrigé en séparant `cancel()` (COMPLETED uniquement, manuel) de `expireAwaitingPayment()` (AWAITING_PAYMENT uniquement, appelée exclusivement par le worker), bug préexistant depuis S22 pour MOBILE_MONEY mais jamais couvert par un test de timing réel avant cette session. (2) *revue sécurité dédiée (sous-agent)* — `TenantPaymentGatewayService.verifyWebhookSignature`/`generatePaymentLink` traitaient l'absence de `PaymentGatewayCredential` (état par défaut de toute organisation n'ayant pas configuré son agrégateur, contrôlable en libre-service via `PUT /organizations/settings/payment-gateway`) comme un « mode test » acceptant toujours la signature — contrairement à `PaymentAggregatorService.isTestMode` (gardé par des variables d'environnement posées par l'exploitant, jamais par un tenant), cela permettait à n'importe quel appelant public du webhook de confirmer un paiement CARD/MOBILE_MONEY jamais réellement effectué pour toute organisation sans agrégateur configuré — corrigé (`isPlatformTestMode()` gardé par `NODE_ENV !== 'production'`, webhook rejeté et lien de paiement refusé plutôt que mode test silencieux en production), tests de verrouillage dédiés ajoutés. IDOR/isolation tenant confirmées à plusieurs niveaux : `organizationId` toujours vérifié serveur dans `confirmAsyncPayment`/`confirmPayment`/`expirePayment` et le contrôleur de credentials ; test e2e dédié prouvant qu'une signature calculée avec le secret d'une organisation A est rejetée pour une organisation B même avec de vrais identifiants configurés des deux côtés. Tests : suite unitaire API 773/773 (69 suites, dont `payment-gateway-credential.service.spec.ts`, `tenant-payment-gateway.service.spec.ts`, `async-payment.service.spec.ts`, `sale-online-payment.service.spec.ts`, extensions `pos.service.spec.ts`/`sale.service.spec.ts`/workers) + suite e2e API 391/391 (33 suites, dont `payments-webhook.e2e.spec.ts` généralisé, `payments-webhook-tenant-isolation.e2e.spec.ts`, `sale-online-payment.e2e.spec.ts`, `payment-gateway-credential.e2e.spec.ts`, extension `pos.e2e.spec.ts` — compétition confirmation/expiration CARD). `pnpm --filter api lint`/`typecheck`/`build` verts, aucune régression. |
| S32 | `NotificationsModule` — `email-queue` (Nodemailer) + templates | Un email de facture part réellement via la queue | ✅ 2026-08-23 — La file BullMQ `email` et `EmailService` (Nodemailer + `SmtpServerService`) existaient déjà depuis S24 — non reconstruits. Cette session : (1) habillage brandé commun `wrapBrandedEmail` (`branded-email.template.ts`, logo + `primaryColor` de l'organisation, repli sur le vert brand `#2FA75E` de `tailwind.config.ts` si absent ou hors format HEX 6 chiffres déjà imposé par `UpdateBrandingSchema`) appliqué aux 5 renderers (les 2 existants — `sale-message.renderer.ts`/`quotation-message.renderer.ts`, S24/S28 — **retrofit dans cette session pour rester cohérents**, et les 3 nouveaux) ; `formatAmount`/`escapeHtml`/`formatDate` factorisés dans `message-format.util.ts` (dupliqués jusqu'ici entre les deux renderers existants) ; (2) couverture étendue à `Purchase` (`purchase-message.renderer.ts`), `PaymentSale`/`PaymentPurchase` (`payment-receipt-message.renderer.ts`, un seul renderer avec discriminant `kind: 'sale' | 'purchase'` — mirrors exacts en base) et `SaleReturn`/`PurchaseReturn` (`return-message.renderer.ts`, même patron `kind`) ; `EmailService` étendu de `sendPurchaseSummary`/`sendPaymentReceipt`/`sendReturnSummary`, la logique d'envoi commune (transporteur Nodemailer + gestion d'erreur, dupliquée entre les 2 méthodes existantes) factorisée dans un `deliver()` privé partagé par les 5 méthodes ; workers dédiés `purchase-email.worker.ts`, `payment-receipt-email.worker.ts` (un seul worker, deux noms de job `paymentSale.sendEmail`/`paymentPurchase.sendEmail`) et `return-email.worker.ts` (idem `saleReturn.sendEmail`/`purchaseReturn.sendEmail`), tous `@Processor('email')` sur la file existante. **Investigation préalable (étape bloquante du prompt)** : retry exponentiel déjà en place (`MessagingQueueModule`, `attempts: 3`/`backoff` exponentiel 5s sur `email` et `sms`) — non retouché ; `QueueEventsListener` absent, et pas seulement pour `email` — **aucune queue du projet n'en a** (recherché sur tout `apps/api/src`), écart architecture/code signalé plutôt que tranché en silence. Le seul précédent réel pour « notifier l'échec via Socket.io » est `BackupWorker` (T09), qui émet directement `server.to('org:{id}').emit(...)` depuis son `catch`, sans classe `QueueEventsListener` dédiée — patron suivi ici (`email:sendFailed` émis vers la room d'organisation dans le `catch` de chacun des 5 workers de la file `email`, y compris les 2 existants) plutôt que d'introduire un concept absent du reste du code ; cibler précisément l'utilisateur déclencheur (`org:{id}:user:{userId}`, lecture littérale de la doc) aurait exigé d'ajouter `userId` aux payloads de job de S24/S28 déjà en production — **décision validée explicitement avec l'utilisateur avant codage**, tranchée en faveur du patron existant. **Deuxième décision validée avant codage** : le prompt liste 4 nouvelles routes `/send` dans son plan de tests (achat, un seul paiement, retour-vente, retour-achat) mais le renderer/`EmailService` de reçu de paiement couvrent structurellement `PaymentSale` ET `PaymentPurchase` de façon symétrique — **5 routes livrées** (`POST /purchases/:id/send`, `POST /sales/payments/:id/send`, `POST /purchases/payments/:id/send`, `POST /sale-returns/:id/send`, `POST /purchase-returns/:id/send`), toutes en mirror exact du patron `SaleController.send` (S24) : permission `*.view` réutilisée (pas de nouvelle permission), pas de body/DTO (un seul canal email pour ces 4 nouveaux types de document, contrairement à Sale/Quotation qui gardent email+SMS), 400 si le contact (email `Client`/`Provider`) est manquant. **Exclusions actées dans le plan de session, non improvisées ici** : réinitialisation de mot de passe (aucun flux « mot de passe oublié » dans `AuthModule` — ce n'est pas une session « templates email », à traiter dans une session Auth dédiée si retenue) ; génération PDF (S34, `pdf-queue`, séparée). **Régression latente trouvée et corrigée avant qu'elle ne casse quoi que ce soit** : ajouter `MessagingQueueModule` à `PurchasesModule`/`ReturnsModule` (nécessaire pour `@InjectQueue('email')` dans les nouveaux `.send()`) aurait fait échouer la compilation NestJS de 4 suites e2e existantes (`purchase.e2e.spec.ts`, `payment-sale.e2e.spec.ts`, `sale-return.e2e.spec.ts`, `purchase-return.e2e.spec.ts`, `payment-return.e2e.spec.ts`) qui montent `PurchasesModule`/`ReturnsModule`/`SalesModule` sans `BullModule.forRootAsync` — corrigé en l'ajoutant aux 5 fichiers (mirror exact `sale.e2e.spec.ts`) avant que la régression ne soit constatée en CI ; cleanup `afterAll` de `purchase.e2e.spec.ts` complété (`paymentPurchase.deleteMany` manquant avant `purchase.deleteMany`, aurait violé une contrainte FK dès qu'un test crée un paiement). Tests : 5 nouveaux specs renderers/template (`branded-email.template.spec.ts`, `purchase-message.renderer.spec.ts`, `payment-receipt-message.renderer.spec.ts`, `return-message.renderer.spec.ts`, extension `email.service.spec.ts`), 5 nouveaux specs workers (`purchase-email.worker.spec.ts`, `payment-receipt-email.worker.spec.ts`, `return-email.worker.spec.ts`, extensions `sale-email.worker.spec.ts`/`quotation-email.worker.spec.ts` — branding + notification d'échec), extensions `.send()` sur les 5 specs de service concernés (contact manquant → `BadRequestException`, IDOR → `ForbiddenException`, introuvable → `NotFoundException`) ; e2e : les 5 nouvelles routes (202 si contact renseigné, 400 sinon, 404, 403 isolation tenant) réparties dans `purchase.e2e.spec.ts` (2 describe), `payment-sale.e2e.spec.ts`, `sale-return.e2e.spec.ts`, `purchase-return.e2e.spec.ts`. `pnpm --filter api lint`/`typecheck` verts, `pnpm --filter api test:unit` 838/838 (76 suites) et `pnpm --filter api test:e2e` 412/412 (34 suites, dont `worker.e2e.spec.ts` qui boote le vrai graphe DI de `WorkerModule` avec les 3 nouveaux workers enregistrés), aucune régression. **Test manuel d'envoi réel (étape 8 du prompt) non effectué** : le conteneur Docker `ensemb-worker-1` tourne en crash-loop depuis 5 semaines (image périmée, problème pré-existant documenté dans `worker.e2e.spec.ts`, sans rapport avec cette session) — le vérifier en conditions réelles aurait exigé de reconstruire les images Docker api/worker, une action sur l'infra qui tourne ; **décision validée avec l'utilisateur** de considérer la couverture automatisée (e2e prouvant l'enfilage réel des jobs, tests unitaires des workers prouvant le rendu brandé + l'appel `EmailService`, smoke-test du vrai graphe DI) suffisante plutôt que de toucher l'infra pour ce gain marginal. |
| S33 | `sms-queue` (Twilio) | Un SMS de facture part réellement via la queue | ✅ 2026-08-23 — La file BullMQ `sms` et `SmsService` (Twilio) existaient déjà depuis S24 (`sendSaleSummary`/`sendQuotationSummary`) — non reconstruits. Cette session étend la couverture SMS aux 3 types de documents ajoutés en email par S32 : achat, reçu de paiement (vente+achat), retour (vente+fournisseur). 3 nouvelles fonctions de rendu (`renderPurchaseSmsBody`/`renderPaymentReceiptSmsBody`/`renderReturnSmsBody`, mirror exact de `renderSaleSmsBody` — texte court à une ligne, référence/montant/statut uniquement, ni nom de contrepartie ni détail des lignes) ; `SmsService` étendu de `sendPurchaseSummary`/`sendPaymentReceipt`/`sendReturnSummary` (mirror exact `sendSaleSummary`, même transport Twilio, même mode test) ; 3 nouveaux workers dédiés sur la file `sms` existante — `purchase-sms.worker.ts` (mirror simple `sale-sms.worker.ts`), `payment-receipt-sms.worker.ts` et `return-sms.worker.ts` (mirror des workers email doubles de S32, mais **sans** `PrismaService` de chargement de branding/noms de produits/contrepartie pour `return-sms.worker.ts` ni de branding pour `payment-receipt-sms.worker.ts` — le rendu SMS ne les utilise pas, économie assumée par construction plutôt que code mort). Les 5 routes `/send` livrées email-only par S32 (`POST /purchases/:id/send`, `/purchases/payments/:id/send`, `/sales/payments/:id/send`, `/sale-returns/:id/send`, `/purchase-returns/:id/send`) acceptent désormais `channel: 'email' | 'sms'` dans le body (mirror exact du contrat déjà en place sur `/sales/:id/send`/`/quotations/:id/send`) — DTOs mutualisés à l'intérieur de chaque module plutôt que dupliqués cinq fois : `SendPurchaseSchema` (nouveau, `purchases/dto/`) réutilisé par `PurchaseController` et `PaymentPurchaseController` ; `SendSaleSchema` (existant, S24) réutilisé tel quel par `PaymentSaleController` ; `SendReturnSchema` (nouveau, `returns/dto/`) réutilisé par `SaleReturnController` et `PurchaseReturnController`. Chaque service vérifie désormais `phone` (Client/Provider, champ déjà présent en base) avant d'enfiler sur la file `sms`, 400 explicite sinon, mirror exact de la garde déjà en place côté email. Répartition en agents experts comme prescrit par le plan de session (renderers bloquants, backend achats / backend paiements-retours en parallèle, tests, revue finale) ; **deux agents en arrière-plan ont calé sans laisser de rapport complet** (`stream watchdog`, même symptôme déjà rencontré en S30) — le travail partiel de chacun a été vérifié puis complété directement plutôt que retenté une troisième fois : l'agent achats n'avait touché aucun des fichiers qui lui étaient assignés (repris intégralement : `purchase.service.ts`/`purchase.controller.ts`/`purchase-sms.worker.ts`/`SmsService.sendPurchaseSummary`) ; l'agent tests s'était arrêté juste avant la partie e2e après avoir livré les tests unitaires (renderers/`SmsService`/3 workers, tous vérifiés verts). **Une régression bloquante trouvée et corrigée avant qu'elle n'atteigne les tests existants** : `channel` devenant obligatoire dans le body des 5 routes ci-dessus, les 19 appels e2e existants qui faisaient `.send()` sans argument (S32, alors email-only sans DTO) seraient tous tombés en 422 — corrigés en `.send({ channel: 'email' })` sur les 5 fichiers concernés (`purchase.e2e.spec.ts` ×2 describe, `payment-sale.e2e.spec.ts`, `sale-return.e2e.spec.ts`, `purchase-return.e2e.spec.ts`) avant d'ajouter la couverture SMS proprement dite (202 sms, 400 sans téléphone, 422 channel invalide/absent, contacts de test dotés d'un `phone`). Un deuxième défaut plus mineur trouvé en vérifiant le travail des agents : 4 des 5 fichiers de spec unitaires (`payment-purchase.service.spec.ts`, `payment-sale.service.spec.ts`, `sale-return.service.spec.ts`, `purchase-return.service.spec.ts`) avaient bien le paramètre `channel: 'email'` ajouté aux appels `service.send()` existants mais **sans jamais enregistrer le mock de la queue `sms`** dans le module de test NestJS — la suite `payment-purchase.service.spec.ts` échouait entièrement à l'injection de dépendances (`Nest can't resolve dependencies... BullQueue_sms`) ; corrigé sur les 4 fichiers, tests SMS ajoutés en complément (par ailleurs nécessaires pour ne pas laisser la variable `smsQueue` déclarée-mais-inutilisée, détecté par le lint). Tests : 3 nouveaux specs renderers étendus, extension `sms.service.spec.ts` (3 nouvelles méthodes, mirror `sendQuotationSummary`), 3 nouveaux specs workers (`purchase-sms.worker.spec.ts`, `payment-receipt-sms.worker.spec.ts`, `return-sms.worker.spec.ts`), extensions des 5 specs de service (branche sms : job enfilé, 400 sans téléphone) ; e2e : les 5 routes étendues avec channel sms (202/400/422), régression des 19 appels existants corrigée. `pnpm --filter api lint`/`typecheck` verts, `pnpm --filter api test:unit` 886/886 (79 suites) verts. **`test:e2e` non exécuté dans cet environnement** : le moteur Prisma ne parvient pas à joindre le Postgres Docker local (`Can't reach database server at 127.0.0.1:5433`) alors qu'une connexion TCP brute au même port réussit instantanément — reproduit avec un appel Prisma minimal sans rapport avec cette session (échec identique hors sandbox outillage), signalé explicitement plutôt que masqué ; le code e2e (régression corrigée + nouveaux tests sms) est écrit, type-checké et lint-vert mais n'a pas pu être exécuté contre une vraie base — à vérifier en priorité dans un environnement où `test:e2e` tourne normalement avant de considérer la session pleinement close. |
| S34 | `pdf-queue` (Puppeteer) branché sur ventes/achats/devis/retours | PDF généré et téléchargeable pour chacun des 4 documents |
| S35 | `SettingsModule` : `Setting`, `PosSetting`, `SmtpServer` (config dynamique en base) | SMTP changeable depuis l'UI sans toucher à un fichier |

### Bloc I (= P9) — Rapports & dashboard (dépend de tous les modules métier ; volumineux, donc très découpé)

| # | Contenu | Fait quand… |
|---|---|---|
| S36 | Rapports clients (5 endpoints) + écran | Montants cohérents avec les ventes/paiements sources |
| S37 | Rapports fournisseurs (4 endpoints) + écran | Idem côté achats |
| S38 | Rapports ventes/achats/top produits + graphiques | Graphiques cohérents avec les filtres de date |
| S39 | Rapports par entrepôt (6 endpoints) | Cohérents avec le stock et les documents de l'entrepôt |
| S40 | Profit & perte, rapport du jour, dashboard, cache Redis, `dashboard:refresh` | Flow §18.13 complet, cache invalidé sur événement métier |

### Bloc J (= P10) — Permissions avancées côté UI (dépend du Bloc B)

| # | Contenu | Fait quand… |
|---|---|---|
| S41 | Écran de gestion rôles/permissions (89 droits groupés par domaine) + vérification bout en bout de `records.viewAll` sur tous les modules documentaires | Flow §18.11 complet, testé sur au moins 3 modules différents |

### Bloc K (= P11) — Réglages système, sauvegarde (dépend du Bloc H pour les réglages)

| # | Contenu | Fait quand… |
|---|---|---|
| S42 | Écrans réglages (société, POS, SMTP) | Branchement complet sur `SettingsModule` (S35) |
| S43 | `BackupModule` (`pg_dump`, `backup-queue`, purge) + écran | Flow §18.12 complet |

### Bloc L (= P12) — Application mobile (dépend du Bloc E et du Bloc B2 pour la résolution de tenant, idéalement des Blocs F/G aussi)

| # | Contenu | Fait quand… |
|---|---|---|
| S44 | Init Expo, design system (`nativewind`), écran de résolution de tenant (identifiant d'organisation avant login, §10), navigation | Login mobile fonctionnel contre la même API, brandé à l'organisation résolue |
| S45 | Écran POS mobile (scan caméra, §17 point D) branché sur l'API existante | Flow §18.2 rejoué sur mobile |
| S46 | Impression thermique mobile + repli `expo-print` | Reçu imprimé sur imprimante Bluetooth réelle ou PDF de repli |
| S47 | Pipeline `eas build`/`eas submit` intégré au CI (§12.3) | Build mobile déclenchable depuis la CI |

### Bloc M (= P13) — Frontend web complet & polish (dépend de tous les blocs backend)

| # | Contenu | Fait quand… |
|---|---|---|
| S48 | Écrans restants non couverts par les blocs précédents (people/users, résiduels) | Tous les modules de §5 ont un écran |
| S49 | i18n `fr`/`en`, thème clair/sombre, audit d'accessibilité | Contrastes et focus visibles validés |

### Bloc N (= P14) — Recette, migration, mise en production (dépend de tous les blocs précédents)

| # | Contenu | Fait quand… |
|---|---|---|
| S50 | Script de migration MySQL → PostgreSQL + table de correspondance d'IDs (§17 point A) — exécution finale du script éprouvé en S30b | Migration testée sur un jeu de données réel, relations intactes |
| S50b | Test de restauration PITR complet sur environnement jetable + validation des objectifs RPO/RTO (§12.8, §17 point AB) | Une base restaurée à un instant T arbitraire est vérifiée cohérente (comptages, intégrité référentielle, échantillon de documents) |
| S51 | Exécution intégrale de la checklist §15 | Toutes les cases cochées |
| S52 | Pipelines de déploiement `staging`/`prod`, monitoring, bascule (§12.3/§12.5) | Bascule effectuée, dashboards actifs |

> **Comment utiliser ce découpage :** ouvrir une session = annoncer son numéro ("on attaque S14"), relire uniquement les sections référencées (pas tout le document), livrer jusqu'à la définition de "fait", committer, passer à la suivante. Si une session déborde largement sa portée annoncée, c'est le signal qu'elle doit elle-même être re-découpée avant de continuer.
